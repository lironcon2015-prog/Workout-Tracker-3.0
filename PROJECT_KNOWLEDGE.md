# GYMPRO ELITE — Project Knowledge

> מסמך זה מכיל רק מידע שאינו נגזר מקריאת הקוד: החלטות, לקחים, מלכודות.

---

## גרסה נוכחית: 19.7.1

### v19.7.1 — ברירת מחדל ביומן: הכמות מהפעם האחרונה

**מה השתנה:** פתיחת עורך הכמות למזון מוכר מציגה את **הכמות והיחידה שבהן הוא תועד
לאחרונה**, במקום ברירת המחדל הגנרית (מנה מוגדרת אם יש, אחרת 100 גרם). מזון חדש —
התנהגות ישנה ללא שינוי.

**איפה נשמר:** `lastPortion: {qty, unit, unitLabel}` על רשומת המזון ב-`KEY_FOOD_DB`,
דרך `setFoodLastPortion`. נשמר גם ב**עדכון** רשומה קיימת ולא רק בתיעוד חדש — תיקון
כמות הוא האינדיקציה החזקה ביותר למה שהמשתמש באמת אוכל.

**למה נשמרת התווית ולא האינדקס:** למזון יכולות להיות כמה מנות (סקופ / 100 גרם),
ורשימת ה-`servings` משתנה אחרי "עריכת ערכי המוצר". אינדקס שמור היה מצביע אחרי שינוי
כזה על **מנה אחרת**. `_fdUnitToPickerVal` פותר את היחידה לפי התווית, ונופל ל-`s0`
כשהתווית כבר לא קיימת.

**באג קיים שתוקן אגב כך:** עריכת רשומה שתועדה במנה השנייה (`s1`) הציגה את הראשונה —
הקוד היה `entry.unit === 'serving' ? 's0'`, אינדקס קבוע. עכשיו שתי הזרימות (עריכה
ו"אחרון") עוברות דרך אותו פותר תוויות.

**הגנות:** כמות 0 או לא-מספרית נדחית ולא דורסת ערך קיים; כתיבה רק למזון שכבר במאגר
(רשומה חלקית ללא שם היא אותה מלכודת שנתפסה בהשלמת הסיבים).

**חוב טכני:** ה"אחרון" גלובלי למזון ולא לפי ארוחה. 30 גרם שיבולת שועל בבוקר ו-100
בערב יידרסו זה את זה. `mealUse` כבר קיים ברשומת המזון אם נרצה בהמשך פירוט לפי ארוחה.

### v19.7.0 — המאמן יוצר מזון מותאם: בלוק מובנה, לא פרסור פרוזה

**הבקשה:** אחרי שהמאמן מעריך ערכים תזונתיים למוצר — טריגר שייצור ממנו מזון מותאם.
ההצעה שעלתה: להתחבר לזיהוי המלל של יוצר המזון.

**ההכרעה — התחברות ליוצר המזון נכונה, אבל דרך תעבורה אחרת.** בקוד קיימות שתי
מערכות רלוונטיות, והנכונה אינה זו שהוצעה:
- **זיהוי מלל** (`_fdParseNutritionText` + `_fdAiParseLabelText`) — רגקס/LLM מעל
  **פרוזה**. שם המוצר, יחידת המידה, גודל המנה והסיבים אובדים ברגקס על משפטים.
- **בלוק מובנה** — תבנית `<propose-memory>` של תיבת הזיכרון: הפרומפט מורה למודל לפלוט
  בלוק מדויק, `_extractMemoryProposals` מסיר אותו מהתצוגה ומחזיר דאטה, ומסך אישור
  נפתח. **זו ארכיטקטורת "המאמן מציע → המשתמש מאשר → המערכת כותבת" בקוד הזה.**

**המימוש:** `<propose-food name unit kcal protein carbs fat fiber serving />` בפרומפט,
`_extractFoodProposal` באותה תבנית בדיוק, ונחיתה ב**טופס המזון המותאם הקיים** מלא-מראש
(`fdOpenFoodFromProposal` → `_fdShowCustomFoodForm(null)` + מילוי שדות). **אפס מסלול
שמירה חדש** — המשתמש מאשר/מתקן ולוחץ "שמור בלבד"/"שמור ותעד" כמו בכל מזון מותאם.

**משמעת הפרומפט** מועתקת מתיבת הזיכרון: בלוק רק כשהתבקש, אסור להכריז "שמרתי",
סיבים ריקים אם לא ידועים (אל תנחש), ולשאול במקום לנחש כשחסרים נתונים.

**נפילה לאחור לשיחות ישנות:** בועת מאמן שהפרוזה שלה נקראת כערכים תזונתיים מקבלת שבב
"צור מזון" שמזרים אותה לאותו זיהוי מלל — כלומר ההצעה המקורית נשמרה, כמסלול משני.
סף מכוון: קלוריות **ולפחות שני** מאקרו, כדי שאזכור חולף של קלוריות לא ייצר כפתור.

**באג שנתפס בבדיקות — גרש בשם מזון עברי.** `attr` השתמש ב-`["']([^"']*)["']`, ולכן
`name="קוטג'"` נחתך ל-`קוטג`. שמות מזון בעברית מלאים בגרש (קוטג', צ'יפס, ג'לי). תוקן
ללכידת התוחם עם backreference: `(["'])([\s\S]*?)\1`.
**לקח:** תבנית תווים-שאינם-תוחם שבירה בעברית; תמיד ללכוד את התוחם ולסגור עליו.

**בדיקות:** 18 על החילוץ (בלוק מלא, ריק≠אפס, אפס אמיתי, תגית סוגרת, בלוקים פגומים,
גרשיים בודדים, דו-קיום עם `<propose-memory>`, וגרש עברי) + 27 ביקורת חיווט.

### v19.6.5 — המלצת המאמן זמינה גם ממסך הלייב

**מה נוסף:** שבב "המלצה" בשורת הכותרת של כרטיס "סט נוכחי" ב-Live. פותח את
`#set-rec-sheet` **הקיים** — לא נבנה מנגנון חדש, רק נקודת כניסה שנייה.

**למה בכרטיס הנתונים ולא בכותרת** (שם הוא יושב ב-`ui-main`): ההמלצה משנה בדיוק את
המספרים שבכרטיס. נקודת הכניסה יושבת על מה שהיא מזיזה. נכנס ל-`.live-target-head`
שכבר `flex` עם `gap:8px` — אפס פיקסלים נוספים למסך, וה-header נשאר שורה נקייה.
מוקאפ: `docs/mockup-live-recommendation.html`.

**שתי נקודות שהיו חייבות טיפול, ושתיהן מהסוג שנשכח:**

1. **מקור אמת יחיד לשתי החזיתות.** לטריגר יש כעת שתי חזיתות, ולכן כל גישה ישירה
   ל-`getElementById('set-rec-trigger')` הוחלפה ב-`_setRecTriggers()` שמחזיר את שניהם.
   שלוש נקודות: תנאי ההצגה (קיום מפתח Gemini ב-`_resetSetRecState`), חסימה בזמן בקשה,
   ושחרור החסימה. בלי זה היה נוצר כפתור מת במסך אחד, או כפתור שנשאר חסום.
2. **סנכרון אחרי "אשר".** `applyAIRecommendation` כתב לפיקרים וקרא ל-`syncStepperDisplay`,
   שמעדכן את `ui-main` **בלבד**. אישור המלצה מתוך ה-Live היה מעדכן את הפיקרים ומשאיר
   את המספר בכרטיס "סט נוכחי" הישן. נוספו `_syncLiveEditSheetDisplays()` +
   `updateLiveViewContent()`. זהו **כלל אחידות הפיקרים** בהוראות הפרויקט, במופע נוסף.

**מה לא נדרש:** שכבות. `body.live-mode-active .bottom-sheet` כבר מקבל `z-index:10006`
מול `9999` של ה-overlay, כך שהגיליון נפתח נכון מעל הלייב מלכתחילה.

**stopPropagation:** הכרטיס כולו נושא `onclick="openLiveEditSheet()"`, ולכן השבב חייב
לעצור את הבועה — אחרת לחיצה עליו הייתה פותחת את עורך הסט במקום את ההמלצה.

### v19.6.4 — ברקוד הוא מזהה מוצר, לא מזהה מקור

**התסמין:** מוצר ישראלי שנקלט מסריקת תווית לא קיבל סיבים, בעוד שמוצר סרוק **אחר**
דווקא כן. שני התסמינים יחד הצביעו על הסיבה.

**הסיבה:** בסריקה יש שני מסלולים שונים. אם זוהה ברקוד והמוצר נמצא ב-OFF —
`resolveBarcode` מחזיר מזון עם `source:'off'`, וההשלמה עבדה. אם Gemini קרא את ערכי
התווית ישירות (המוצר לא ב-OFF, או שלא זוהה ברקוד) — המזון נשמר עם `source:'gemini'`,
ו-`_fdBackfillFiber` חסם אותו בתנאי `(!food.source || food.source === 'off')`.

**הטעות בתפיסה:** התייחסתי ל-`source` כאילו הוא קובע אם מותר לפנות ל-OFF. אבל
**הברקוד הוא מזהה המוצר** — מוצר עם ברקוד הוא אותו מוצר ב-OFF בלי קשר לאיך נקלט
אצלנו. התנאי החליף "מאיפה הגיע" ב"מה הוא".

**מדוע זה בטוח:** ההשלמה היא מילוי שדה **חסר** בלבד — הפונקציה יוצאת מיד כשקיים ערך
(כולל אפס אמיתי), `edited` חסום, ו-kcal/מאקרו לא נוגעים בשום מקרה. התנאי החדש חוסם
רק `custom` ו-`meal` (ערכים שהמשתמש בנה בעצמו).

**חצי שני של הבעיה — מוצר שפשוט אינו ב-OFF.** נפוץ במוצרים ישראליים, ואז ההשלמה
נכשלת בשקט מוחלט. נוסף חיווי בתצוגה המקדימה של הפיקר: כשמוגדר יעד סיבים והמזון בלי
נתון — "סיבים — הוסף בעריכת ערכי המוצר", בדיוק מעל הכפתור שפותר את זה. מוצג רק כשיש
יעד (סימן שהמדד מעניין), כדי לא להרעיש על כל מזון.

**בדיקות:** 13 בדיקות ענפים על `_fdBackfillFiber` האמיתי (עם סטאבים לרשת): שישה
מסלולים שנכנסים להשלמה, שישה שנחסמים במכוון, ואי-כתיבה למאגר של מזון שאינו שם.

### v19.6.3 — ביקורת כיסוי מלאה לסיבים: חמישה חורים שהתגלו

ביקורת שיטתית של **כל מקור × כל מסלול** אחרי שצמ"ת תוקן. חמישה פערים, כולם בקוד
שכתבתי ב-19.6.0–19.6.2:

1. **`_fdAddComponentFromFood`** — הוספת מזון כמרכיב ב-Meal Builder בנתה `per100`
   מחדש בלי `fb`. מזון **עם** סיבים איבד אותם ברגע שנכנס למנה מורכבת. דאטה חדשה.
2. **רשומת "יחידה" → מרכיב** (עריכת רשומה קיימת) — אותו באג, נתיב אחר.
3. **`_fdUpsertSavedMeal`** — ה-`per100` של הארוחה השמורה עצמה נבנה בלי `fb`.
4. **עורך המרכיבים** — ארבעה שדות (קל'/ח/פ/ש) ואין סיבים, כך שמרכיב שהגיע בלי נתון
   לא היה ניתן להשלמה **בשום דרך**. נוסף תא חמישי; ריק = לא ידוע, לא אפס.
5. **`_fdBackfillFiber` כיסה רק OFF+צמ"ת.** נוספו:
   - `basic:<i>` — **פתרון מקומי מיידי בלי רשת.** הטבלה המובנית נושאת סיבים מאז
     19.6.1, אבל עותקים שנשמרו לקאש לפני כן לא. השוואה לפי id והשלמה במקום.
   - `usda:<fdcId>` — **תיקון לטענה שגויה שלי.** אמרתי למשתמש שה-`fdcId` אינו נשמר
     ולכן USDA לא ניתן להשלמה. הוא **כן** נשמר, בתוך ה-`id`. נוסף `lookupUsdaFood`.

**לקח:** כשמוסיפים שדה למודל נתונים, `grep` על **כל בניות האובייקט** (`per100: {`)
ועל כל ההעתקות — לא רק על נקודות הכניסה. שלושה מהחמישה היו פונקציות שבונות `per100`
מחדש במקום להעתיק אותו, וכל אחת מהן שמטה את השדה בשקט.

**כיסוי סופי — כל מקור נושא סיבים:** OFF, צמ"ת, USDA, חומרי גלם מובנים, מזון מותאם
ידני, סריקת תווית, הדבקת טקסט (מקומי + AI), הערכת AI לפי שם, צילום מנה, ומרכיבי
Meal Builder. **לא מכוסים במכוון:** ימי MFP (החלטת משתמש), גשר Health, ורשומות עבר
(`per100` מוקפא).

**בדיקות:** 33 בדיקות ביקורת סטטית על הקוד האמיתי (מיפוי כל מקור, נוסח הפרומפטים,
שרשרת החישוב, כל ענפי ההשלמה, והפרדת הסיבים מחישוב הקלוריות) + 20 בדיקות התנהגות
+ 13 על סורק צמ"ת + 6 אילוצים על טבלת חומרי הגלם.

### v19.6.2 — סיבים מצמ"ת: שם עמודה שנוחש, ולמה זו הייתה טעות

**התסמין:** מנגו מצמ"ת נמשך בלי סיבים.

**הסיבה:** ב-19.6.0 מיפיתי את הסיבים מצמ"ת לפי **ניחוש** של שם העמודה
(`total_dietary_fiber` → `dietary_fiber` → `fiber`), כי הרשת חסומה בסביבת הפיתוח ולא
יכולתי לאמת מול ה-API. אף אחד מהשלושה לא תפס.

**מה שכן היה נכון:** הבקשה לצמ"ת אינה מגבילה `fields`, ולכן **כל** עמודות הטבלה כבר
חוזרות ברשומה. הנתון תמיד הגיע — רק לא ידענו איך קוראים לו.

**התיקון — לא לנחש שם, לסרוק:** `_tzFiber(r)` עובר על מפתחות הרשומה ומחפש דפוס
(`fib(er|re)` או "סיב"), מחריג תת-קטגוריות (`soluble`/`insoluble`/"מסיס") שאינן הסך
הכולל, ומעדיף מפתח שמכיל `total`/`dietary`. עמיד לשינוי שמות עתידי. כשלא נמצאה עמודה
כלל — `console.debug` חד-פעמי מדפיס את רשימת העמודות, כדי שאיתור הבעיה הבא ייקח דקה.

**לקח:** כשאי אפשר לאמת סכימה חיצונית — לזהות לפי **דפוס**, לא לפי שם קשיח. שם קשיח
שנוחש נכשל בשקט; סריקת דפוס נכשלת ברעש (ה-debug) ומצליחה ברוב הווריאציות.

**הרחבה נלווית:** `_fdBackfillFiber` כיסה רק OFF-לפי-ברקוד, ולכן מזון צמ"ת מהקאש
("אחרונים"/"מועדפים") היה נשאר בלי סיבים לנצח. נוסף `lookupTzameret(sml)` — שליפה
בודדת דרך `filters` על `smlmitzrach`, המזהה שכבר שמור ב-`id` כ-`tz:<sml>`. USDA נשאר
בחוץ: ה-`fdcId` אינו נשמר על המזון, ואין מזהה יציב לשליפה חוזרת.

### v19.6.1 — סגירת פער הכיסוי של הסיבים

**הפער שהתגלה מיד אחרי 19.6.0:** הטאבים "אחרונים" ו"מועדפים" (`fdRenderTab`) קוראים
**ישירות מ-`KEY_FOOD_DB`** ואינם עוברים ברשת. מזון שתועד משם המשיך להיכתב בלי סיבים גם
אחרי שהמיפוי תוקן — וזה בדיוק המסלול השכיח ביותר בתיעוד יומיומי. "קדימה בלבד" עבד רק
בחיפוש מחדש, לא בשימוש חוזר.

**נמצא גם הצד החיובי:** בחיפוש הקאש **כן** מרפא את עצמו. `_fdDedup` שומר את המופע
הראשון, ו-`netStrong` (תוצאת רשת טרייה) מופיע לפני `cacheStrong` — כך שחיפוש מחזיר את
הגרסה החדשה עם הסיבים, ובחירה בה כותבת `fb` למאגר.

**תוקן:**
1. `_fdBackfillFiber` — נקרא מ-`_fdOpenPortion`. מזון מהקאש עם ברקוד ובלי `fb` נמשך
   מ-OFF ברקע, המאגר מתעדכן והתצוגה מתרעננת. **קדימה בלבד** — משלים את המזון במאגר,
   לא רשומות עבר (ה-`per100` מוקפא בכל רשומה). הגנות: לא נוגע ב-`source !== 'off'`,
   לא ב-`edited` (ערך שתוקן ידנית), אחת למזון (`_fdFbTried`), ורק כשהמזון כבר במאגר —
   upsert על מזון שאינו שם היה יוצר רשומה חלקית ללא שם. כשל/אופליין = שקט מוחלט.
2. `_BASIC_RAW` — עמודת `fb` ל-68 חומרי הגלם המובנים, מ-USDA SR Legacy (אותו מקור שממנו
   נבנו `k/p/c/f`). **25 מהם אפס אמיתי** (בשר/דגים/חלב/שמן/סוכר) — וזה שווה ערך לכיסוי
   מלא, לא ל"חסר". 43 נושאים ערך.

**סייג לתיעוד:** הערכים נכתבו מידע USDA ולא ממשיכה חיה — הרשת חסומה בסביבת הפיתוח
(403 מהפרוקסי ל-`api.nal.usda.gov`, `world.openfoodfacts.org`, `data.gov.il`). נבדקו
אוטומטית מול אילוצים: סיבים ≤ פחמימות בכל שורה, טווח 0–30, אפס במזון מן החי, ערך
משמעותי בקטניות/אגוזים/דגנים מלאים, ומלא > מזוקק. ראויים לבדיקה נקודתית מול המקור.

### v19.6.0 — סיבים תזונתיים ביומן (יומן בלבד)

**מה נוסף:** אינדיקציית סיבים יומית ביומן המזון + יעד יומי, באותה מתודולוגיה של המאקרו.

**מיקום התצוגה (חלופה ג׳ מתוך חמש שנשקלו):** תת-שורה בתוך **עמודת הפחמימה** בכרטיס
הסיכום — `סיבים 21+/32` ב-0.52rem. הסיבים הם תת-קבוצה של הפחמימות, ולכן זה מקומם
התזונתי הנכון, וזה גם המיקום שלא נותן להם מרכזיות של מאקרו רביעי. **ללא צבע ייחודי**
(החלטת המשתמש) — `--text-dim` כמו שאר המטא בכרטיס. בשורת הרשומה: שבב רביעי `Fb 8`,
מוצג **רק כשיש נתון**.

**החריג החישובי — הכלל החשוב ביותר כאן:** הסיבים **אינם** נכנסים ל-`_macroTargetKcal`
(`P×4 + C×4 + F×9`) ולא ל-`_fdKcalMismatch`. הם כבר סגורים בתוך ה-`carbs` ברוב מסדי
הנתונים; ספירה שנייה הייתה מנפחת את יעד הקלוריות (~64 קק"ל על יעד 32 ג׳) ומסמנת כל
מזון עתיר-סיבים כ"קלוריות לא תואמות". שדה `th-fb` בעורך היעדים ההיסטורי במכוון **בלי**
`oninput="_thMacroInput()"` מאותה סיבה.

**חסר אינו אפס — יישום שני:** `_fdRn()` נוסף לצד `_fdR()` ומשמר `undefined` במקום
להחזיר 0. רשומה בלי `fb` = "לא ידוע", ולא "אפס סיבים". הכיסוי נספר (`complete/total`)
ומוצג כ-**`+`** ליד המספר (`21+/32` = "לפחות 21, יש רשומות בלי נתון"), עם tooltip
מפורט. אפס אמיתי (שמן זית) נשמר כ-`0` ונספר ככיסוי מלא. זה אותו כלל שתיקן את באג
ה-00:00 בשינה ב-19.5.3 — הפעם הוחל **מראש**, בעיצוב ולא בתיקון.

**מקורות שנושאים סיבים:** Open Food Facts (`fiber_100g`), USDA (nutrient 1079), צמ"ת,
מזון מותאם ידני, סריקת תווית/הדבקת טקסט/הערכת AI (הפרומפטים מנחים מפורשות להחזיר
`null` ולא לנחש), ומנות Meal Builder (נגזר מהמרכיבים, עם `fbPartial` כשמרכיב אחד חסר).

**מקורות שאינם נושאים (מכוון):** ימי MyFitnessPal — העמודה קיימת ב-CSV הגולמי תחת
`micros` אך **הוחלט לא לחלץ**; גשר Apple Health; טבלת `_BASIC_RAW` המובנית — לא הומצאו
לה ערכים. בכל אלה תת-השורה תציג `—` או `+`, לא מספר שקרי.

**ייצוא מאוחד:** `fiber` נוסף לפריטי `nutrition_detailed` (+`fiberPartial`), ו-`fiber`/
`fiberKnown`/`fiberEntries` ל-`nutrition_daily` דרך `recomputeNutritionDay`. ה-`readme`
מזהיר מפורשות שאין להשלים שדה חסר ב-0.

**מלכודת שנתפסה:** `fiberTarget` היה נעלם בכל שחזור/סנכרון ענן — רשימת המיזוג הלבנה של
`analyticsPrefs` ב-`storage.js` (~שורה 1050) היא whitelist מפורשת, ומפתח שלא נמצא בה
פשוט לא עובר. **לקח:** כל pref חדש חייב להתווסף לרשימה הזו באותו commit.

### v19.5.3 — "המאמן ראה 00:00 שינה": אפס שהתחזה למדידה

**התלונה:** נכנסתי לאפליקציה, התחלתי אימון בלי לעבור במסך הבריאות, ובסיכום המאמן טען
שהוא רואה 00:00 זמן שינה — למרות שהנתונים היו באפל ובגשר.

**האבחנה נגזרה מהמספר עצמו.** `fmtH` ב-`_buildSleepAIContext` מחזיר `—` על `null` ומקטע
ריק כשאין רשומות בכלל. `00:00` יכול לנבוע **רק** מרשומת לילה קיימת עם `asleepMin: 0`.
לא הזיה של המודל, ולא תזמון המשיכה.

**שורש: אפס נשמר כמדידה.** אפל מחשבת HRV/דופק לפני ששלבי השינה נכתבים, ולכן דחיפת
בוקר/אמצע-יום מגיעה עם `asleepMin: 0`. ב-`mergeSleepDays` היא עברה את פילטר הלילה-הריק
דרך `_hasVitals`, ונשמרה כאפס. חמור מזה — **לא הייתה הגנת fill-only על שדות השינה**:
`Object.assign({}, existing, d)` נתן לאפס לדרוס לילה אמיתי שכבר נקלט. זה בדיוק הבאג
שכבר תועד ותוקן עבור `wristTempDev` ("נצפה בפועל: ערך שנקלט נעלם למחרת") — אבל ההגנה
מעולם לא הוחלה על השינה עצמה.

**למה רק המאמן ראה את זה:** כל שכבת התצוגה והציון מסננת מדדים דרך `_validVital`
(`asleepMin: [30,900]`), ולכן אפס מנוטרל מהציון ומה-baseline. **הפרומפט למאמן היה
הצרכן היחיד שקרא ערכים גולמיים** והצהיר עליהם כעובדה. `_slFmtDur` בתצוגה סבל מאותה
חולשה (בודק `null/NaN` בלבד) — כלומר גם המסך היה מציג `0:00` + "מתחת ליעד".

**לקח מרכזי:** כשמוסיפים שכבת סינון תקינות (`_validVital`) — היא חייבת לחול על **כל**
הצרכנים, כולל אלה שאינם UI. פרומפט ל-AI הוא צרכן נתונים לכל דבר, והוא המסוכן שבהם:
הוא לא מציג "0:00" למשתמש שיזהה את השטות — הוא **מנמק** ממנו.

**תוקן (5 נקודות):**
1. `mergeSleepDays` — fill-only לשדות השינה: ערך תקין נכנס גובר, אפס רק ממלא מקום ריק,
   ואם אין מה למלא השדות נמחקים (`null` = חסר, לא 0). זהירות שנשמרה: כשה-`asleepMin`
   הנכנס אפס אבל השלבים תקינים — הלילה **כן** נקלט (שינה = סכום השלבים).
2. `_buildSleepAIContext` — כל המדדים דרך `_validVital`; שינה חסרה מייצרת אזהרה מפורשת
   ("חסר, לא אפס — אין לצטט 00:00") + נפילה למדידה התקינה האחרונה עם תיוג גילה.
3. `renderSleepView` — `—` + "טרם נמשך מ-Apple" במקום `0:00`, גם בכרטיס היעילות ובשלבים.
4. `renderBodyLog` — לא רינדר את תצוגת השינה כלל. `_applyTabVisibility` רק הסיר
   `display:none`, כך שכניסה ל-Composition עם תת-הטאב "שינה" פעיל חשפה HTML מרינדור
   קודם. נתונים שנמשכו בכניסה לאפליקציה לא הופיעו עד החלפת תת-טאב.
5. `syncHealthNutrition` — `_healthSyncLast` נקבע **לפני** הבקשה ולא הוחזר בכשל, ולכן
   משיכת-כניסה שנפלה חסמה כל ניסיון אוטומטי ל-15 דק' בשקט מוחלט. נוסף rollback +
   ניסיון חוזר אחד אחרי 20ש' בכניסה. בנוסף: רינדור מחדש גם כש-`changed=0`, כדי
   שחותמת "מסונכרן · עודכן HH:MM" תזוז אחרי משיכה מוצלחת.

### v19.5.2 — לוח השנה נכנס לסטריפ של הארכיון

**מה השתנה:** הבורר במסך הארכיון עבר מ-3 לשוניות + כפתור אייקון נפרד ל**סטריפ מלא של 4**:
אימונים / שקילות / תזונה / לוח שנה. `.archive-cal-btn` ו-`#btn-view-calendar` הוסרו.
מוקאפ: `docs/mockup-archive-strip.html`.

**למה זה לא היה באמת "שני צירים":** על הנייר `archiveSubTab` ו-`archiveView` היו
אורתוגונליים, אבל `setArchiveTab` תמיד קרא ל-`switchArchiveView('list')` — כלומר בחירת
תת-מסך *כבר* סגרה את לוח השנה. האיחוד רק יישר את ה-UI למה שהלוגיקה עשתה ממילא.

**החלטת מודל המצב:** לוח השנה **אינו** נשמר ב-`archiveSubTab`. הוא נשאר ב-`archiveView`,
ו-`archiveSubTab` ממשיך להחזיק רק את שלושת תתי-המסכים של הדאטה. התוצאה: יציאה מלוח
השנה נוחתת מעצמה על תת-המסך האחרון שהיית בו, בלי לזכור נקודת חזרה בנפרד.
חישוב הלשונית הדולקת רוכז ב-`_syncArchiveSegActive`, שנקראת מ-`switchArchiveView` —
נקודת הסנכרון היחידה, ולכן `toggleArchiveCalendar` הישן ממשיך לעבוד ומדליק את הלשונית.

**מלכודת שנתפסה:** בורר הרקעים (סקריפט inline ב-`index.html`) זיהה את מצב לוח השנה דרך
`#btn-view-calendar.classList.contains('active')` — כלומר דרך האלמנט שנמחק. בלי תיקון
הרקע של תת-המסך `archive-calendar` היה מפסיק להתחלף בשקט. `archCalendar()` עודכן לקרוא
את הלשונית הפעילה (`#archive-seg .seg-btn.active`), באותה תבנית כמו `blNutrition()`
לידה. **לקח:** מחיקת אלמנט UI מחייבת grep על ה-id שלו גם בסקריפטים ה-inline של
`index.html`, לא רק בקבצי ה-JS.

**תקדים שהוריד סיכון:** מסך Composition כבר מריץ `segment-wrapper` עם ארבע לשוניות
(שקילה/תזונה/שינה/תמונות) — הרוחב הוכח שם. נוסף `white-space: nowrap` על לשוניות
הארכיון כדי ש"לוח שנה" (הלשונית היחידה עם רווח) לא תישבר לשתי שורות ותקפיץ את הגובה.

**בונוס עקביות:** הכפתור הישן היה אייקון-בלבד, בניגוד לחוק "אין אייקונים על כפתורים".
השינוי מסיר את החריגה.

---

### v19.5.1 — משקל שהוזן ידנית עוגל כלפי מטה בסט הבא

**הבאג שדווח:** בתרגיל עם קפיצות של 1 ק"ג, הזנה ידנית של 12.5 → הסט הבא הציג **12**.

**השורש — שני מנגנונים שנפגשו:**

1. **רשת ה-step עוגנת ב-`defaultW`, לא באפס.** ב-`initPickers` הטווח הוא
   `minW = defaultW - 40`, ולכן הרשת כמעט תמיד *מכילה* את המשקל הנוכחי — גם ערך
   "מוזר" כמו 47.3 בקפיצות 2.5 (הרשת נבנית 7.3, 9.8, … 47.3). לכן הבאג נראה נדיר.
   הוא מתפוצץ רק כש-`Math.max(0, …)` **חותך את minW לאפס** — במשקלים קלים —
   ואז הרשת מתיישרת לאפס והשארית של המשתמש נופלת ממנה.
2. **`_setPickerValue` נצמד לשכן הקרוב, ובתיקו בוחר את הנמוך.** ההשוואה היא
   `diff < bestDiff` (חד-משמעי), ולכן 12.5 — שמרוחק 0.5 גם מ-12 וגם מ-13 —
   נפל תמיד על הראשון שנסרק, כלומר כלפי מטה.

**התיקון:** ערך שאינו יושב על הרשת **מוזרק כ-option משלו** (`_insertSortedOption`,
אותה פונקציה שמשרתת את ההזנה הידנית) במקום להיצמד לשכן. הצמדה לקצה נשמרה רק
עבור ערך שנמצא **מחוץ** לטווח הבורר — שם היא ההתנהגות הנכונה.

**למה זה נכון מבחינת חוזה המשתמש:** ערך שהוזן ידנית הוא הצהרה מפורשת, ואסור
שייעלם בשקט. הוא נשמר לכל הסטים הבאים וגם לאימון הבא (דרך `saveWeight`), ומשתנה
רק בפעולה יזומה — הזנה חדשה, או `stepPicker` שמזיז לערך הרשת השכן.

**שתי החזיתות מכוסות אוטומטית:** ה-`<select>` הוא מקור האמת המשותף למסך המפורט
ול-`live-edit-sheet` (`editLivePickerValue` קורא ל-`commitCustomValue`,
`_liveStepPicker` קורא ל-`stepPicker`) — התיקון ב-`initPickers` חל על שתיהן.

---

### v19.5.0 — סבב (Cluster): החלפת תרגיל תוך כדי, מנוחה בסבב נוסף, סדר בעורך

שלושה פערים במנגנון הסבב, שכולם נבעו מאותו שורש: **הסבב הוא הזרימה היחידה שאין בה
מסך ביניים לכל תרגיל.** המעבר בין תרגילי הסבב קורה בתוך `nextStep` עצמו, ולכן כל מה
שתלוי ב-`ui-confirm` פשוט לא קיים שם.

**1. אי-אפשר היה להחליף תרגיל אחרי הכניסה לסבב.**
כפתור ההחלפה (`#btn-swap-confirm`) חי רק ב-`ui-confirm`. בסבב המסך הזה מוצג פעם אחת
בלבד — כמסך מבוא — ושם `.secondary-buttons-grid` מוסתר במפורש. מרגע הכניסה לסבב לא
הייתה שום נקודת גישה.

*הפתרון:* פריט "החלף תרגיל" ב-`#workout-quick-menu`, מוצג רק כש-`clusterMode` פעיל
**וגם** המסך הפעיל הוא `ui-main` (ב-`ui-cluster-rest` אין "תרגיל נוכחי" ו-`clusterIdx`
כבר לא רלוונטי). המסך `ui-swap-list` הקיים מקבל ענף מודע-סבב.

*החלטת היקף:* ההחלפה חלה על **שאר הסבבים באימון הנוכחי**, לא על סבב בודד.
מחליפים תרגיל כי המכונה תפוסה או כי משהו כואב — שתי סיבות שלא נעלמות בסבב הבא,
והחלפה חד-פעמית הייתה מכריחה לחזור על הפעולה בכל סבב.
זה בטוח כי `state.activeCluster` הוא `deepClone` של פריט התוכנית (ראה `checkFlow`) —
**התוכנית השמורה לעולם לא נוגעת.** מוצגת הערה מפורשת על כך בראש מסך ההחלפה.
סטים שכבר נרשמו שומרים את שם התרגיל המקורי: זה תיאור נאמן של מה שבוצע בפועל.

*שלוש מלכודות שהתגלו במימוש:*
- **`navigate()` הורג את טיימר המנוחה.** שורה `if (id !== 'ui-main') stopRestTimer()`
  אומרת שעצם פתיחת מסך ההחלפה מאפסת מנוחה שרצה. הפתרון: snapshot של
  `state.startTime` לפני המעבר, והמשך **מאותה נקודה** בחזרה (לא איפוס) —
  `resetAndStartTimer` ואז דריסת `state.startTime` בערך השמור. השחזור תלוי ב-`initPickers`,
  שהוא הנקודה המשותפת לשני מסלולי היציאה: בחירת תרגיל וגם "ביטול" דרך `_doBack`.
- **`isCalc` חייב כיבוי מפורש.** בסבב `confirmExercise` מכבה אותו, וההחלפה עוקפת את
  `confirmExercise` לגמרי. בלי הכיבוי, החלפה לתרגיל MAIN מהמאגר הייתה נכנסת למסלול
  ה-1RM המחושב באמצע סבב.
- **מקטע "החלף סדר עם תרגיל אחר" היה מסוכן בסבב.** הוא נשען על
  `state.workouts[type][state.exIdx]`, ובסבב `exIdx` מצביע על **בלוק הקלאסטר כולו** —
  לחיצה שם הייתה מחליפה את הסבב הרץ עם בלוק אחר באמצע הריצה. בסבב הוא מוחלף
  ב-"החלף סדר בתוך הסבב" (סידור פנימי בין תרגילים שטרם בוצעו).

**2. "+ בצע סבב נוסף" לא הפעיל את שעון המנוחה מחדש.**
`handleClusterFlow` מפעיל טיימר רק כש-`clusterRound < rounds`. בסיום הסבב האחרון
התנאי נכשל → `stopRestTimer()`, ו-`renderClusterRestUI` מחליף את התצוגה בסימן ✓.
`addExtraRound` הגדיל את `rounds` ורינדר מחדש — אבל אף אחד לא הפעיל את הטיימר,
וה-✓ נשאר תקוע. `resetAndStartTimer` מכסה את שניהם (הוא מאפס גם את התצוגה והטבעת).

**3. בעורך התוכניות לא היה שינוי סדר בתוך הסבב.**
לבלוקים ברמה העליונה יש `moveExInEditor`, לתרגילים בתוך הסבב היה רק כפתור מחיקה.
נוסף `moveExInCluster` + חצים מנוטרלים בקצוות. התוויות (`A1`/`B2`) והאינדקסים
ב-`onclick` נגזרים מחדש בכל `renderEditorList`, ולכן החלפה במערך מספיקה.

---

### v19.4.7 — תיקון כולל של מנגנון הניווט וכפתור "חזור"

**הבאג שדווח:** סיום תרגיל במסך Live → מסך הביניים → לחיצה על "חזור" בטעות →
לולאה שלא ניתן להתקדם ממנה.

**השורש:** ‏`handleBackClick` על `ui-confirm` לא ניווט אחורה אלא **מחק סט מהלוג**
והחזיר את המשתמש לתרגיל הקודם:
- תרגיל `isCalc` (Main) → `showConfirmScreen()` = אותו מסך בדיוק ⇒ לולאה עצמית שקטה,
  כל לחיצה מוחקת עוד סט בלי אישור ובלי שינוי ויזואלי.
- תרגיל רגיל → `navigate('ui-main')` ⇒ ping-pong `ui-confirm ⇄ ui-main`, ובמצב Live
  ה-overlay נפתח מחדש בכל סיבוב.
- כל עוד הלוג לא ריק, "חזור" **מעולם** לא הגיע ל-`_doBack` — כלומר לא הייתה דרך
  לצאת מהאימון בלי למחוק אותו סט-סט.

**ההחלטה:** "חזור" ממסך הביניים = יציאה מהאימון (עם אישור כשיש דאטה). מחיקת/עריכת
סט נשארת ביומן הסשן (תפריט ⋯ → "יומן" → הקשה על סט → "מחק סט זה") — אפשרות
גלויה ומכוונת, לא תופעת-לוואי של כפתור חזרה.

**עשרה תיקונים נוספים באותו מהלך:**
| # | באג | תיקון |
|---|-----|-------|
| 1 | `navigate()` דחף כפילויות לערימה (ארכיון⇄פרטים, `ui-variation` בפריסטייל, `ui-1rm` שנשאר מתרגיל שהסתיים) ⇒ הערימה גדלה בלי גבול ו"חזור" נחת על מסכים מתים | Single-instance: מסך שכבר בערימה גורם ל**קיצוץ** אליה במקום push |
| 2 | `_doBack` לא רינדר מחדש את מסך היעד (רק `ui-variation`) ⇒ מסכי ביניים/סבב/מנהל עם תוכן ישן | `_refreshScreen(id)` — switch מרכזי, משותף עם `restoreSession` |
| 3 | `handleBackClick` קרא את המסך הנוכחי מראש הערימה ולא מה-DOM ⇒ כל drift הריץ ענף של מסך אחר | `_activeScreenId()` + `_syncStackToActiveScreen()` (ריפוי-עצמי) |
| 4 | `state.setIdx--` בלי מחיקה מהלוג ⇒ סטים כפולים בארכיון אחרי "חזור" באמצע תרגיל | `_undoLastSetOfCurrentEx()` (כולל דרופים משורשרים) + `setIdx` נגזר מהלוג |
| 5 | יציאה מאימון ניקתה רק את האחסון ⇒ `state.workoutStartTime` נשאר, ‏`_persistOnTeardown` כתב מחדש את ה-session, וההפעלה הבאה הציעה לשחזר אימון שננטש | `_abandonWorkout()` — איפוס מלא של דגלי ה-state |
| 6 | "חזור" ממסך הסיכום → `ui-ask-extra` → "סיום" שוב ⇒ **רשומת ארכיון כפולה** (`finish()` איפס `archivedTimestamp`) | `ui-summary` ו-`ui-ask-extra` ב-`NO_BACK_SCREENS` + `archivedTimestamp` לא מאופס |
| 7 | `getNextExerciseName()` ניגש ל-`state.workouts['Freestyle']` שלא קיים ⇒ **TypeError** בסיום כל סט אחרון בפריסטייל, וזנב `_advanceAfterSet` (עצירת טיימר, `_syncDropUI`, `updateLiveViewContent`) לא רץ | ענף `isFreestyle` + `|| []` |
| 8 | ‏`_applyScreenChrome` לא סנכרן את ה-tab-bar ⇒ `enterBgEdit()` הציג אנליטיקה עם הטאב "אימון" מסומן | סנכרון לפי `UI_ROOT_TABS` + `enterBgEdit` עבר ל-`switchMainTab` |
| 9 | סוואייפ-חזרה לא נחסם מעל `.slog-overlay` (יומן הסשן) — הוא אינו `.modal-overlay` | הרחבת ה-selector ל-`.modal-overlay, .slog-overlay, .bottom-sheet-overlay` |
| 10 | `saveArchiveEdit`/`exitArchiveEditMode` ניווטו ל-`ui-archive` בלי `openArchive()` ⇒ הרשימה הציגה את הסיכום שלפני העריכה | קריאה ל-`openArchive()` אחרי הניווט |

**כללי-אצבע שנגזרו:** "חזור" לעולם לא no-op (מבוי סתום מחזיר הביתה), לעולם לא מוחק
דאטה בלי אישור, ולעולם לא נוחת על מסך שלא רונדר מחדש. יציאה מאימון עוברת תמיד
דרך `_abandonWorkout()`.

**כיסוי בדיקות:** 110 בדיקות Playwright על מסך אמיתי — הזרימה שדווחה, Live,
פריסטייל, הפרעה, שלב תרגילים נוספים, Main/1RM, סבב, דרופ, עורך התוכניות, ארכיון,
שחזור סשן, ו-fuzz של 120 ניווטים אקראיים (תמיד מסך פעיל אחד, ערימה חסומה).

### v19.4.6 — תזכורת עדכון TM בשבוע 1

**הצורך:** TM מוגדר בהגדרות חוסך הזנת 1RM כל אימון (v16.78), אבל בדיוק בגלל זה
הוא "נשכח" — בלוק שלם רץ על TM ישן בלי שאף מסך מזכיר אותו.

**הכלל:** בתחילת תרגיל MAIN בשבוע 1, אם ה-TM לא עודכן 7+ ימים — שאלה לפני
שהסטים מחושבים. "כן" פותח את מסך הפיקר הקיים (ברירת מחדל על ה-TM הנוכחי,
כותרת "עדכון TM") והערך נשמר גם כ-TM קבוע בהגדרות; "לא" ממשיך כרגיל.

**החלטות שאינן נגזרות מהקוד:**
- **חותמת נפרדת** (`KEY_EXERCISE_TM_AT`) ולא שינוי צורת הערך ב-`KEY_EXERCISE_TM` —
  הצורה `{exName: number}` נקראת בייצוא, בייבוא ובסנכרון הענן; אובייקט מקונן היה
  שובר את כולם.
- החותמת נכתבת **רק ב-`saveExerciseTM`** (עדכון יזום של המשתמש). שחזור מענן/ייבוא
  כותבים ישירות ל-key ולכן לא "מרעננים" TM ישן.
- **אין חותמת = ראוי לבדיקה.** כל ה-TM-ים הקיימים ייכנסו לתזכורת בפעם הראשונה —
  זו התוצאה הרצויה, לא באג.
- `state.tmUpdateMode` נדלק/נכבה **בתוך `setupCalculatedEx`** לפי ה-override, לא
  בפונקציה הפותחת — יציאה מהמסך באמצע לא תגרום להזנת 1RM הבאה להיכתב כ-TM קבוע.
- שאלה אחת לתרגיל בכל אימון (`_tmReviewAskedInSession`, מתאפס ב-`selectWorkout`).

### v19.4.4–19.4.5 — טאב תמונות: סדר הכרטיסים + בוררי סגמנט

סדר הכרטיסים שונה ל: פעולה ← קלט ← תוצאה ← מצב יציב. כרטיס ההשוואה (התפריטים
שקובעים מה ינותח) והתוצאה שלו היו מתחת לקו הקיפול, והמשתמש לא ידע שיש שם משהו —
מה שנקרא כ"המערכת מתעלמת מהבחירה". סדר ה-DOM אינו משפיע על הרינדור (כל כרטיס
נגיש דרך `getElementById`). בנוסף `.segment-wrapper`/`.seg-btn` עברו מ---r-xs
ל---r-pill: 8px על גובה ~40px נקרא כמלבן, והבורר היה החריג היחיד בשפה שכולה pill.

## גרסה קודמת: 19.4.3

### v19.4.3 — "בחירה" מול "ברירת מחדל": שתי ברירות מחדל שלא ידעו אחת על השנייה

v19.4.2 הסיק "אד-הוק" מהשוואת הזוג הנבחר לזוג האוטומטי — בלי לדעת אם המשתמש **בחר**
או שכך התפריט נפתח. ברירת המחדל של כרטיס ההשוואה הייתה "הקרוב ביותר ל-14 יום אחורה"
(`_ppDefaultCompareDate`), וזו כמעט אף פעם לא הזוג של מנוע הניתוח ("הקודמת במרחק ≥1 יום").
התוצאה: החיווי הכריז "השוואה שביקשת" על ברירת מחדל, ו-**"נתח עכשיו" לעולם לא עדכן את
ציר-הזמן** — כל לחיצה נפלה למסלול האד-הוק. הפוך מהכוונה.

**הלקח:** כשמצב UI הופך לקלט של לוגיקה, "ברירת מחדל" ו"בחירה" חייבים להיות **שני מצבים
מובחנים**. השוואת ערכים לא מספיקה — ערך זהה יכול להגיע משני מקורות בעלי משמעות הפוכה.

**הפתרון:** דגל `_ppCompareTouched` (`ppSetCompare` מרים אותו). לא נגע → התפריטים עוקבים
אחרי זוג הניתוח האוטומטי בכל רינדור, כך שהתפריטים/החיווי/הניתוח תמיד מדברים על אותו זוג.
נגע → הבחירה דביקה וגוברת על כל רינדור ועל צילום תמונה חדשה; מתאפסת רק אם התמונה הנבחרת
נמחקה. `_ppDefaultCompareDate` נשמרה ללא קורא (לא נמחקה ללא החלטה) — חוב טכני קטן.

### v19.4.2 — השוואת תמונות: הבחירה לא הגיעה לניתוח + הפרדת אד-הוק מציר-הזמן

**הבאג:** כרטיס ההשוואה כיבד את הבחירה בתפריטים (`_ppCompareSel`), אבל `ppAnalyzeNow()`
קרא ל-`_ppPickAnalysisSet()` שתמיד בחר את התמונה האחרונה מול הקודמת לה. הכפתור יושב
מעל כרטיס ההשוואה, ולכן נראה קשור לבחירה — ולא היה. `_ppCompareSel` לא הופיע כלל במנוע הניתוח.

**ההחלטה (למה לא רק "להעביר את הבחירה לניתוח"):** `entries` + `aiNotes` הם זיכרון
מגמה **כרונולוגי**. השוואה חד-פעמית היא שאילתה, לא נקודה במגמה — כתיבתה לציר-הזמן
פוגעת בשלושה מקומות: תקרת 30 הרשומות נשחקת, `recent.slice(-5)` מזין למודל חמישה
ורדיקטים על זוגות אחרים (עיגון בפרומפט שכל האתוס שלו "ברירת מחדל: אין שינוי"),
ו-`lastAnalyzedDate` נדרס — מה שמבטל את הניתוח השבועי האוטומטי לתמונה החדשה.

**הפתרון — שני מסלולים:** `_ppResolveAnalysisSet(manual)` מחזיר גם `adhoc`.
זוג זהה לאוטומטי (או ריצה שבועית שקטה) → ציר-זמן קנוני כרגיל. זוג שונה → נשמר
ב-`trend.adhoc` בלבד (רשומה יחידה, נדרסת), לא נוגע ב-entries/aiNotes/חותמות,
ולא מקבל `recent` בפרומפט. מוצג בכרטיס נפרד "השוואה שביקשת" עם כפתור "נקה".

**לקח נלווה:** `_ppFillCompareImages` לא ניקה `src` לפני משיכה — כשל משיכה מהדרייב
השאיר את תמונת הבחירה הקודמת על המסך בלי חיווי, מה שנראה בדיוק כמו "המערכת מתעלמת
מהבחירה". נוסף טוקן דור (כמו ב-`ppOpenViewer`) + הודעת "התמונה לא זמינה".

### v19.4.1 — שינוי סכמת גרסאות: X.Y.Z

עברנו ל-patch (`19.4` → `19.4.1` → `19.4.2`). minor נשמר לפיצ'ר משמעותי בלבד.
הסיבה: כל commit העלה minor, ו-19 "נגמר" מהר מדי.

### v19.4.1 — תרגילים תחליפיים ידניים: מודל "override" במקום מיגרציה

`substituteGroups` ב-`data.js` נשאר מקור ברירת המחדל ולא נגענו בו. ההגדרה הידנית
יושבת ב-`ex.subs` על אובייקט התרגיל, ו-`getSubstitutes` בוחר ביניהם: **קיים `subs`
(גם ריק) → הוא הזוכה; אין → ברירות המחדל**. כך לא נדרשה מיגרציה חד-פעמית, ומשתמש
שלא נגע בכלום מקבל בדיוק את ההתנהגות הישנה.

- **למה `ex.subs` ולא מפתח storage חדש:** `KEY_DB_EXERCISES` כבר נמצא בייצוא הקונפיג,
  בגיבוי המלא ובסנכרון הענן. שדה על האובייקט הקיים רוכב על כל אלה בחינם.
- **`_materializeSubs` — הלב של הדו-כיווניות.** תחליף הוא יחס סימטרי. כשמוסיפים B ל-A,
  צריך להוסיף גם A ל-B; אבל אם ל-B אין `subs` משלו, הכתיבה תיבלע — כי בקריאה הבאה
  `getSubstitutes(B)` יחזור לקרוא את הקבוצות מ-`data.js` ויתעלם מהשינוי. לכן לפני כל
  שינוי בצד השני **מקבעים** לו את ברירות המחדל שלו כרשימה ידנית.
- **`defaultSubstitutes` מאחד את כל הקבוצות** (`filter().flat()`), במקום ה-`find` הישן
  שהחזיר רק את הראשונה. `Machine Press` ו-`Weighted Pull Ups` מופיעים בשתי קבוצות
  וחצי מהתחליפים שלהם פשוט לא הוצג עד v19.4.1.
- **`isExOrVariationDone` עבר גם הוא ל-`getSubstitutes`** — כלומר הגדרה ידנית משפיעה
  לא רק על ההמלצות במסך ההחלפה אלא גם על **דילוג אוטומטי** על תרגיל שתחליף שלו בוצע.
  החלטת משתמש מודעת, לא תופעת לוואי.
- **שלמות הפניות:** רשימות התחליפים מחזיקות **שמות**, לא ids. לכן שינוי שם ומחיקת
  תרגיל חייבים לעדכן/לנקות את כל מערכי `subs` — אחרת נשארות הפניות יתומות.

### v19.2 — מלכודת RTL: ערך נושא-סימן

`+0.4°` הוצג כ-`0.4°+`. הסיבה: `+` ו-`−` הם תווים **ניטרליים** ב-bidi. כשהמספר עומד לבדו
בתיבה (בלי טקסט עברי אחריו), האלגוריתם פותר את הסימן לפי כיוון הפסקה — RTL — וזורק אותו
לקצה הנגדי. **זו הסיבה שאותה בעיה לא מופיעה בשורות הדלתא** (`-5 מול baseline`): שם הטקסט
העברי שאחרי המספר מקבע את הסדר.

**התיקון:** `direction: ltr` על תיבת הערך **בלבד** (`.sl-metric .v.v--ltr`), דרך פרמטר
`ltr` ב-`_slMetric`. לא גלובלי בכוונה — שאר הכרטיסים (`ms 58`, `bpm 47`) מסתמכים על
הסדר ה-RTL הקיים והיו משתנים.

**כלל:** כל ערך מספרי שעשוי לשאת `+`/`−` ומוצג ללא טקסט צמוד — חייב תיבת LTR משלו.

### v19.1 — טמפ' עור: גרירה + פיצול סף הצגה/שקלול

**הבעיה:** אחרי שהנתונים תוקנו (v19.0 + ייצוא רחב), עדיין הוצג "—". שתי סיבות עצמאיות:
הכרטיס קרא **רק את הלילה האחרון**, ו-Apple כותבת את דגימת הטמפ' באיחור ולא בכל לילה;
ובנוסף סף יחיד של 14 לילות חסם הצגה של נתון שכבר היה קיים.

**שני ספים במקום אחד** — הצגה ושקלול אינן אותה דרישה:
`TEMP_SHOW_MIN_NIGHTS = 5` (תצוגה, הרף ש-Apple עצמה עובדת לפיו) מול
`TEMP_MIN_NIGHTS = 14` (שקלול בציון — **לא השתנה**). שקלול לתוך ציון מורכב רגיש לרעש
הרבה יותר מהצגת מספר, ולכן החמרה שם מוצדקת; הסתרת נתון קיים במשך שבועיים אינה.

**גרירה:** `wristTempDev: 3` ב-`VITAL_CARRY_MAX` — אותה מכניקה של RHR/HRV. הערך הנגרר
אינו נשמר ואינו נכנס ל-baseline (אפס זיהום), רק מוצג עם תווית גיל.

**שני חיוויים שחלים יחד:** "מלפני יום · ראשוני" — הערך ישן *וגם* הבסיס טרם מבוסס.
הסתרת אחד מהם מציגה את המספר כאמין יותר ממה שהוא.

**עקביות עם המאמן:** `_buildSleepAIContext` יושר לאותו סף הצגה (5), עם סימון מפורש
"בסיס ראשוני — N לילות, לא משוקלל בציון". אחרת נוצר מצב שהמתאמן רואה מספר על המסך
והמאמן טוען שאין לו נתון.

### v19.0 — טמפרטורת ריסט נמחקה בשקט בסנכרון חוזר

**התסמין:** כרטיס "טמפ׳ עור" הציג "—" למרות שבאייפון היו נתונים יומיים שבוע שלם.

**שתי סיבות נפרדות שהצטברו:**

1. **חלון הייצוא (צד האייפון).** באוטומציית HAE הוגדר `Date Range: Today`. שורת שינה מיוחסת
   לפי `sleepEnd` (היקיצה → היום), אבל `apple_sleeping_wrist_temperature` היא דגימה עם חותמת
   זמן משלה שנופלת לרוב על **הלילה הקודם** — ולכן פשוט לא נכללה בייצוא. ייצוא חד-פעמי של
   `Last 7 Days` העלה את הכיסוי מ-2 ל-6 לילות מתוך 8. **מסקנה:** לגשר הזה כדאי טווח של
   יומיים לפחות; `Today` לבדו מפספס מדדים שחותמת הזמן שלהם נופלת אחורה.

2. **מחיקה בשקט (צד האפליקציה).** ב-`mergeSleepDays` הרשימה `['hrv','rhr','respRate']` מגינה
   "ערך תקין קיים גובר", אבל `wristTempDev` לא נכלל בה — **בכוונה**, כי הנימוק היה שטמפ' נמדדת
   רק בשינה ואינה מזוהמת בערכי-ערוּת. הנימוק נכון לגבי **זיהום** ופספס לגמרי את **המחיקה**:
   `Object.assign({}, existing, d)` נתן ל-`null` נכנס לדרוס ערך תקין קיים, והסנכרון רץ כל שעה.
   נצפה אמפירית — ערך של 26.7 השתנה בין שתי משיכות.

**התיקון:** לטמפ' חוק משלה, הפוך מזה של HRV — *ערך נכנס תקין גובר* (הוא עדכני), אבל *ערך
חסר/לא-תקין אינו דורס* ערך קיים.

**לקח כללי:** `Object.assign(existing, incoming)` על מקור שמדווח מדדים **חלקית** הוא מנגנון
מחיקה, לא מנגנון עדכון. כל שדה כזה צריך החלטה מפורשת: מי גובר כשהנכנס ריק.

### v18.9 — הדיבאונס של גיבוי החיבורים הפך לנגרר (trailing)

**הבאג:** אחרי שתיבת הזיכרון נכנסה ל-`_connectionKeys()` (v18.6), עריכה של כמה כללים ברצף
שלחה קובץ **רק על הראשון**. `maybeBackupConnections` דילג על כל קריאה בתוך 120 שניות מהשליחה
המוצלחת האחרונה — והדילוג היה *זריקה*, לא דחייה. המשתמש ערך את התיבה ולא קיבל כלום, בלי שום חיווי.

**למה זה נראה תקין בקוד:** ההערה המקורית אמרה "דילוג אינו אובדן — טביעת האצבע לא מתעדכנת,
והשליחה תקרה בטריגר הבא". זה נכון טכנית (פתיחת/יציאה מהאפליקציה), אבל הטריגר הבא יכול להיות
שעות אחר כך. הדיבאונס תוכנן ל"שינוי URL ואז token" — שתי שמירות של **פעולה אחת**; תיבת הזיכרון
היא סדרה של פעולות **נפרדות ומכוונות**.

**התיקון:** דילוג מתזמן שליחה לסוף החלון (`_scheduleConnBackupFlush`) במקום לזרוק. טיימר בודד —
עריכה נוספת רק דוחה אותו, אז רצף עריכות מייצר קובץ אחד בסוף במקום קובץ לכל עריכה או אפס.
ריענון דף מבטל את הטיימר, אבל טביעת האצבע לא התעדכנה ולכן הטריגר הבא עדיין שולח.

**לקח:** דיבאונס על פעולה שהמשתמש תופס כ"שמירה" חייב להיות נגרר. דיבאונס שזורק = פעולה
שנעלמת בשקט, וזה מה שהמשתמש חווה כ"הפיצ'ר לא עובד".

### v18.7 — דרופ סט

דרופ = **המשך של אותו סט** במשקל מופחת. שלוש החלטות שמכתיבות את כל המימוש:
**הנפח נספר במלואו, מונה הסטים לא גדל, ו-1RM מתעלם ממנו.**

**המפתח לספירה הנכונה:** `details[ex].sets` הוא מערך **מחרוזות**, ו-`sets.length` הוא מקור האמת
לספירת סטים בכל האנליטיקה (סטים לשריר, פלאטו, עקביות, כרטיס ארכיון). לכן הדרופ **משורשר לשורת
סט האב** אחרי `⤵` ולא נדחף כרשומה נפרדת — וכל אתרי הספירה נשארו ללא נגיעה. רק `_setStrVol` הורחב
לסכום מקטעים (`split(DROP_ARROW)`), ו-`_setStrVolOne` הוא הפרסר הישן ללא שינוי.
**נלווה בחינם:** `parseSetsFromStrings` (מזין את כל חישובי ה-1RM/PR) תופס את ההתאמה הראשונה
במחרוזת — כלומר את סט האב בלבד. כך דרופים מוחרגים מ-1RM **בלי שורת קוד**.

**מודל:** `drop: 1..3` על רשומת לוג, נדחפת מיד אחרי האב; `_groupSetsWithDrops` משייך לפי סדר
המערך (אין מזהה הורה). בתוכנית: `dropSet` + `dropPct` על פריט התוכנית.
**מלכודת שנתפסה:** `archivedLog` ב-`_saveToArchive` ממפה רשימה לבנה של שדות — בלי להוסיף שם את
`drop`, הדגל נעלם ברגע שהאימון נשמר.
**עוד אחת:** `state.lastLoggedSet` **לא** מתעדכן מדרופ, ו-`saveWeight` לא נקרא ממנו — אחרת משקל
הדרופ היה הופך ל-prefill של הסט הבא ושל האימון הבא.

**מכונת המצבים:** `dropArmed` (מסומן) → `dropMode` (מתעדים) → `dropDone` (פאנל החלטה: המשך /
+ דרופ נוסף). כל ה-UI נגזר מ-`_syncDropUI()` שנקרא מתוך `initPickers` — ולכן `restoreSession`
(שקורא ל-`initPickers`) מחזיר את מצב הדרופ **בלי לוגיקת שחזור נפרדת**. הזנב של `nextStep` חולץ
ל-`_advanceAfterSet(keepTimer)`; `keepTimer` מונע איפוס מנוחה שכבר רצה מאז סיום הדרופ.

**טבעת הטיימר:** ענבר + תווית DROP ויעד 15 שניות בזמן דרופ; חוזרת לירוק ומתאפסת **אחרי** הדרופ
האחרון — שם באמת נגמר הסט. חל על שתי החזיתות (`#timer-area.is-drop`, `body.drop-mode-active`).

**עיגול:** `_roundToDropStep` מעגל **כלפי מטה** לרשת הקפיצות של התרגיל — דרופ קל מדי הוא טעות
זניחה, דרופ כבד מדי מפספס את מטרת הטכניקה.

**מלכודות CSS שנתפסו בהרצה אמיתית (לא בקריאת קוד):**
`#btn-submit-set` מגדיר גרדיאנט כחול בספציפיות ID — מחלקה בלבד לא גוברת עליו, נדרש
`#btn-submit-set.drop-gradient`. ו-`.secondary-action` משתמש ב-`!important`, ולכן גם הכפתור
"+ דרופ נוסף" חייב `!important`. **לקח:** בפרויקט הזה צריך לבדוק ספציפיות מול כללי ה-ID
הקיימים לפני שמניחים שמחלקה חדשה "תעבוד".

**מוחרג במכוון:** אין דרופ בתוך סבב/קלאסטר (`_dropSupported()`), כי הזרימה שם כבר מקוננת בסבבים
וקינון כפול הופך את התיעוד לבלתי קריא. `_syncDropUI` מנקה דגלים שנשארו במעבר לסבב.

**פרומפט המאמן:** נוספה הוראה מפורשת שדרופ אינו סט נוסף ואינו רגרסיה, ושאין לחשב ממנו 1RM.

### v18.6 — תיבת זיכרון: אנטי-כפילויות, גיבוי מיידי, העתקה

**הבאג:** המאמן הציע שוב ושוב כללים שכבר בתיבה. **הסיבה:** הוא stateless — הכללים המאושרים
אמנם מוזרקים לו, אבל אין מנגנון שמונע ממנו לפלוט `<propose-memory>` על משהו שכבר שם, והמערכת
קיבלה כל הצעה בעיוורון. **התיקון בשתי שכבות** (הוראה בפרומפט אינה הגנה — היא רק מקטינה תדירות):

1. **קוד (ההגנה האמיתית):** `findDuplicateMemoryEntry` ב-storage.js — נרמול (lowercase, הסרת
   פיסוק/ניקוד/מקף עברי) + זהות/הכלה + דמיון Jaccard על מילים בסף 0.82.
   `_filterNewProposals` מסנן לפני פתיחת מסך האישור (כולל כפילויות בתוך אותה תשובה) — אם הכול
   כפול, המסך **לא נפתח בכלל**. `addMemoryBoxEntry` מחזיר `{ok:false, reason:'duplicate', existing}`
   גם בהוספה ידנית.
   **מלכודת שנתפסה:** טווח הניקוד `[֑-ׇ]` בולע את המקף העברי U+05BE — חייבים להחליף פיסוק
   ברווח **לפני** מחיקת הניקוד, אחרת "ו־Leg" הופך ל-"וleg" ומילים נדבקות.
   **סף ההכלה:** נחשב כפילות רק כשהקצר ≥60% מהארוך — אחרת כלל קצר חדש שמופיע כביטוי בתוך
   כלל ארוך נחסם בטעות.
2. **פרומפט:** הוראה מפורשת לבדוק את רשימת הכללים המאושרים לפני כל בלוק, וש"הצעה חוזרת היא תקלה".

**גיבוי:** `KEY_MEMORY_BOX` נוסף ל-`_connectionKeys()` — התיבה היא הגדרת מאמן, לא דאטה יומית,
ולכן מקומה בקובץ החיבורים. `saveMemoryBox` (choke point של כל שינוי) קורא ל-`maybeBackupConnections()`
בטריי/קאץ' — כל עדכון תיבה מפעיל ייצוא. הדיבאונס הקיים (120 שניות) מונע הצפה כשמאשרים כמה כללים
ברצף; מה שנדחה יישלח בטריגר הבא, כי טביעת האצבע לא מתעדכנת בלי שליחה מוצלחת.

**העתקה:** `_copyText`/`_copyTextFallback` — עוזר משותף (clipboard API + fallback ל-execCommand
עם `setSelectionRange` ל-iOS). `copyMemoryBoxText()` מעתיק את כל התיבה, `copyAIPersonaText()` מעתיק
את פרומפט הפרופיל **מהטקסטאריאה** (כולל שינויים שטרם נשמרו — מה שרואים הוא מה שמועתק).

### v17.86 — תיבת זיכרון וניסיון

מנגנון קבוע להטמעת כללים שהמאמן ילמד תוך כדי שיחה. **המניע:** ה-LLM stateless — הזיה קלאסית הייתה
"עדכנתי את הוראות הליבה שלי" כשמישהו תיקן אותו. עכשיו: המאמן פולט בסוף תשובה בלוק
`<propose-memory category="…">טקסט</propose-memory>`, הבלוק **מוסתר מהתצוגה**, ונפתח מסך אישור.
המשתמש שומר → הכלל נשמר ל-`KEY_MEMORY_BOX` → מוזרק ב-`buildSystemPrompt` תחת "כללים מאושרים
מתיקונים קודמים" בכל קריאה עתידית (גם slim — לפי בקשת המשתמש).

**מקום בפרומפט:** סעיף חדש `# תיבת זיכרון וניסיון — הצעת כלל קבוע` שמצהיר שהמודל stateless,
מגדיר את פורמט הבלוק, ואוסר להכריז "הוספתי/הטמעתי" בגוף התשובה. סעיף נוסף `# תיקון של המתאמן`
דורש אימות התיקון לפני הודאה (מונע ריצוי אוטומטי), ואוסר להמציא ידע ביומכני.

**תקרות:** 30 רשומות / 3,000 תווים. חורגים → `addMemoryBoxEntry` מחזיר `{ok:false, reason:'full'}`
והמשתמש מתבקש למחוק ידני. **גישה משתמש:** הגדרות ← AI Coach ← "תיבת זיכרון" — סוללת רשומות
עם select קטגוריה + textarea + שמור/מחק, ואזור הוספה ידנית למטה.

**סנכרון:** מוטמע ב-`saveConfigToCloud`/`_applyConfigData` תחת `memoryBox`. **ייצוא מאוחד:**
מוסף כמקטע `memory_box` — לא מסונן בטווח (זה כללים קבועים, לא דאטה יומית).

---


## 🧾 חוב טכני פתוח — מרוכז (עודכן 25.7.2026)

ארבעה פריטים מזוהים, ממתינים להזדמנות. כולם נבחנו והוחלט **במודע** לדחות.

### 1. הגיבוי השבועי אינו מכווץ — 1.2MB על גבול המגבלה

- **נמדד (25.7.2026):** גוף ה-POST של הגיבוי המלא = **1,206KB**. אחרי gzip: **218KB**;
  ‏gzip+base64 (מה שיישלח בפועל): **291KB** — **פי 4.1 קטן יותר**.
- **המשקל:** ‏`archive` 355KB · `food_log` 143KB · `nutrition_raw` 141KB · `ai_history` 131KB.
- **התסמין שהתגלה:** שליחה ידנית החזירה `HTTP 503` עם גוף ריק — כלומר דחייה מחזית גוגל
  **לפני** שהסקריפט רץ. ניסיון חוזר כעבור כמה דקות **הצליח** → מדובר בחנק זמני, לא בחריגה
  קשיחה. אבל המטען יושב על הגבול ו**גדל בכל אימון** — זו פצצת זמן, לא תקלה חד-פעמית.
- **הפתרון המוצע:** ‏`CompressionStream('gzip')` בלקוח (נתמך iOS 16.4+) + base64, ובגשר
  ‏`Utilities.ungzip`. הגשר יקבל **שני הפורמטים** (`backup` רגיל או `gz` מכווץ) כדי שגרסאות
  אפליקציה ישנות ימשיכו לעבוד. בנוסף: לצרף את גודל המטען להודעת השגיאה, כדי שחריגה עתידית
  תזוהה מיד. **דורש פריסה מחדש של `backup-bridge.gs`.**
- **סימן אזהרה שכדאי לנטר:** ‏503 חוזר בשליחה ידנית = הגיע הזמן.

### 2. ~~ששת בלוקי הגשרים~~ — ✅ **בוצע ב-v17.85 (חלקית)**

**ה-JS אוחד:** ‏18 פונקציות (`save/update/toggle` × 6, ~224 שורות) → טבלת `BRIDGES` + שלוש
פונקציות גנריות. ‏`workout-core.js` התכווץ ב-82 שורות נטו. שמות הפונקציות הישנים נשמרו
כעטיפות בנות שורה, כדי שכל אתרי הקריאה הקיימים (`openSettings`, `refreshAllBridgeStatus`,
‏`syncHealthNutrition` ועוד) ימשיכו לעבוד בלי שינוי.
**מה שנשאר פר-גשר בטבלה:** ‏`statusExtra` (טקסט סטטוס נוסף), `syncExtraUI` (תת-מתג התזונה
ב-Health), `extraSave`, ו-`apply` (תופעות לוואי: `_ppKickUploads` / `WatchBridge` /
‏`syncHealthNutrition`). ‏`_watchBridgeApply` נמחקה — הלוגיקה שלה עברה ל-`BRIDGES.watch.apply`.
**נותר פתוח במודע — 134 שורות ה-HTML.** הוחלט לא לייצר אותן דינמית: הערות המידע הן פרוזה
פר-גשר שמקומה ב-HTML, ייצור דינמי מסכן RTL/CSS במסך שכבר נשבר, וה-markup מפסיק להיות
בר-בדיקה בעין. התמורה קוסמטית, הסיכון אמיתי.
**אימות:** harness ב-node שמריץ את שישה הגשרים דרך המסלול הגנרי — שורות סטטוס (כולל
"נשלח לאחרונה"/"נדחף לאחרונה"/"N ממתינות"), שרידות url+token במתג, ארבע תופעות הלוואי,
שמירה עם תת-מתג, ואישור ריקון. 21/21 עברו.

### 3. `loadNutritionRawFromCloud` חסרה — `nutrition_raw` היא write-only לענן

‏`saveNutritionRawToCloud` קיימת ורצה בכל גיבוי, אבל **אין פונקציה שקוראת אותה בחזרה**,
ואין לה שורת UI. ‏`restoreAllFromCloud` לא נוגעת בה. הדאטה בטוחה (מקומית + בכל גיבוי מלא),
ולכן הוחלט לדחות עד שתהיה חזרה לייבוא מ-MFP. שים לב שבאנר הכשל **כן** מכיל תווית עבורה
(`raw: 'קובץ MFP'`) — כלומר הוא יכול להאשים בכשל שאין דרך לתקן.

### 4. רקעים מותאמים אישית אינם בגיבוי המלא

הגיבוי מכסה localStorage בלבד. הרקעים שהמשתמש מעלה חיים ב-IndexedDB (`gympro-bg`), ולכן
שחזור מחזיר את מפתח הבחירה (`gympro-bg-choices-v2`) אך הוא מצביע ל-blob שאינו קיים —
ו-`resolveSrc` נופל לברירות המחדל. תמונות התקדמות **בסדר** (Drive = מקור אמת).

## מסך החיבורים: האחדה + גיבוי חיבורים אוטומטי לאימייל (v17.81)

- **המיפוי שהניע את המהלך:** מדידה על גיבוי אמיתי הראתה שהענן מחזיק **24 מתוך 65** מפתחות.
  כל הסודות (Firebase/Gemini/USDA + 18 מפתחות גשרים), ‏`ai_persona` (4,399 תווים) וההעדפות
  **אינם בענן — במכוון** (אין להעלות טוקנים ל-Firestore). ההיררכיה האמיתית:
  ‏`מייל שבועי ≡ גיבוי מלא ⊇ קובץ חיבורים ⊋ ענן`. אסור שה-UI יציג את הענן כ"הגיבוי".
- **כפילויות שהוסרו:** ‏`manualBackupArchive` הורחבה ב-v17.15 לגבות את **כל ארבעת** המסמכים אך
  נשארה בשורה שכתוב עליה "היסטוריה"; ‏`uploadAllToCloud` עשתה בדיוק אותו דבר; ו-`manualBackupConfig`
  שכפלה את "ייצוא תבנית". שלושתן אוחדו ל-`backupAllToCloud()` יחיד (כולל הגנת "מכשיר ריק"
  ו-`_armSync` שהיו ב-`uploadAllToCloud`).
- **האסימטריה המסוכנת שנסגרה:** אפשר היה לגבות הכל בלחיצה, אבל **לא לשחזר הכל** —
  ‏`restoreAllFromCloud()` הייתה כתובה בקוד **בלי שום כפתור**. נחשפה כ-`restoreAllFromCloud()`
  גלובלי (עטיפה עם אישור + גיבוי אוטומטי לפני).
- **קוד מת שהוסר (אימות: אפס קוראים):** ‏`exportData`, `triggerImport`, `importData`,
  `restoreData`, `getAllData`, `uploadAllToCloud`, `manualBackupArchive`, `manualBackupConfig`.
  ‏**`getAllData` ייצר פורמט קובץ שהמייבא היחיד שלו (`importData`) כבר נמחק** — האפליקציה
  הורידה קובץ "גיבוי" שאין לה דרך לטעון. זה הפח שהמשתמש נתקע בו.
- **גיבוי חיבורים אוטומטי (הדרישה המרכזית):** מ-`_connectionsFingerprint()` — hash על ערכי
  ‏`_connectionKeys()` (ללא הסודות עצמם). כשהוא משתנה, `maybeBackupConnections()` שולחת קובץ
  חיבורים לאותו גשר מייל. **למה טביעת אצבע ולא hook בכל נקודת שמירה:** נקודות השמירה מתרבות
  עם כל גשר חדש, ו-hook שנשכח = מפתח בלי גיבוי. בדיקה מרכזית תופסת גם מפתחות עתידיים.
  טריגרים: ששת שמירות הגשרים, `_bridgeToggle`, Firebase save/clear, מפתחות Gemini/USDA,
  ‏**וכן boot + יציאה מהאפליקציה** כרשת ביטחון. כשל אינו מעדכן את טביעת האצבע → יינסה שוב.
  דיבאונס 2 דק' נגד הצפה; דילוג אינו אובדן מאותה סיבה.
- **‏`backup-bridge.gs` מקבל כעת שני סוגי מטען** (`gympro_full_backup` / `gympro_connections`)
  עם נושא וגוף מייל נפרדים. **דורש פריסת New version** של ה-Apps Script.
- **ערובות לאפס אובדן:** שחזור גיבוי מלא ושחזור מהענן **מורידים אוטומטית גיבוי של המצב הנוכחי
  לפני המחיקה**; ושחזור מקובץ עם פחות מ-70% מהמפתחות שקיימים במכשיר מציג אזהרה מפורשת.
- **נשמר במודע:** ייצוא/ייבוא תבנית וחיבורים **אינם גיבוי** — הם ממזגים בלי למחוק, ולכן הועברו
  ל-sheet "ייצוא/ייבוא מתקדם" במקום להימחק. ‏`nutrition_raw` נשאר write-only לענן (אין
  ‏`loadNutritionRawFromCloud`) — **חוב טכני מודע**, לפי החלטת המשתמש שאינו מייבא מ-MFP כרגע.

## המשך: התצוגה שיקרה — שדות ריקים שנראו כמו מחיקה (v17.80)

- **הממצא שסגר את החקירה:** אחרי v17.79 המשתמש דיווח ששדות הגשרים **שוב** נראו ריקים אחרי
  "גיבוי מלא" — אבל **כיבוי והדלקה של המתג החזירו אותם**. זו ההוכחה שהדאטה **לא נמחקה**:
  המתג קורא ל-`update*BridgeStatus()` שמאכלס מהאחסון. כלומר v17.79 עצר את המחיקה, ומה שנשאר
  היה **באג תצוגה בלבד** — שנראה זהה למחיקה ולכן מבהיל באותה מידה.
- **השורש:** הורדת קובץ ב-PWA של iOS מנווטת את ה-webview החוצה; בחזרה הטופס נבנה מחדש והשדות
  ריקים. מסך ההגדרות **כבר פתוח**, ולכן `openSettings()` (שמאכלס הכל) לא נקרא שוב — ואף אחד
  לא אכלס את השדות.
- **התיקון:** ‏`refreshAllBridgeStatus()` חולץ מ-`openSettings` ונקרא גם ב-`visibilitychange`
  (visible) וב-`pageshow` — רק כשמסך ההגדרות פעיל (`_refreshSettingsIfOpen`) — וגם מתוך
  `exportFullBackup` אחרי 1.2 שניות, למקרה שההורדה כלל לא ניווטה.
- **באג שני באותו דיווח:** ביטול דיאלוג "לרוקן?" לא החזיר את הערך **לתצוגה** (האחסון היה תקין).
  ‏`_bridgeConfirmClear` קיבל פרמטר `restore` רביעי, וכל ששת הקוראים מעבירים את
  `update*BridgeStatus` שלהם.
- **לקח:** כשמצב אמת יושב באחסון והמסך הוא רק בבואה שלו — **כל נתיב חזרה לפרונט חייב לרענן**.
  "הערך קיים באחסון" אינו מספיק; משתמש שרואה שדה ריק מסיק שהוא אבד, ופועל בהתאם (ולוחץ על
  "שחזור" שנמצא בשורה הסמוכה). באג תצוגה על סוד = באג חמור.
- ‏`exportFullBackup` מציג כעת טוסט עם `keyCount` — לחיצה שקטה לא תיראה יותר כמו no-op.

## מחיקת URL/token של כל הגשרים — כתיבה ריקה שעברה את ההגנה (v17.79)

- **התסמין:** אחרי ייצוא "גיבוי מלא", **כל מפתחות הגשרים נמחקו** (MFP/Health/שעון/גיבוי/ווידג'ט/
  תמונות) — אבל **קונפיג Firebase שרד**. קרה פעמיים, שוחזר בכל פעם מקובץ חיבורים.
- **החתימה שפיצחה את זה:** Firebase נשמר ממודל נפרד (`saveFirebaseConfig`), בעוד ששישה הגשרים
  נשמרים **משדות הקלט במסך ההגדרות**. ההבדל הזה מיפה את הבאג ישירות לשכבת ה-UI של ההגדרות.
- **השורש:** ‏`save*BridgeSettings()` שולח תמיד `input.value.trim()` לאחסון, וההגנה ב-StorageManager
  היא `if (url !== undefined)` — **מחרוזת ריקה עוברת אותה ומוחקת את הסוד**. השדות מאוכלסים רק
  בתוך `update*BridgeStatus()` ובתנאי `if (ui && !ui.value)`, כך שב-DOM טרי הם ריקים. ולכל שישה
  המתגים היה `onchange="save*BridgeSettings()"` — כלומר **כל אירוע change על מתג בזמן ששדות
  הקלט ריקים מחק URL+token**. הטריגר בפועל: הורדת קובץ ב-PWA של iOS מנווטת את ה-webview החוצה,
  ובחזרה הטופס נבנה מחדש ומצבי ה-checkbox משוחזרים → change נורה על שדות ריקים.
- **התיקון — שלוש שכבות בלתי-תלויות** (`_bridgeToggle` / `_bridgeConfirmClear` / `_bridgeFill`):
  1. **מתג לא נוגע בסודות.** ‏`toggle*Bridge()` קורא ל-`save*Bridge(on)` עם ארגומנט אחד — url/token
     נשארים `undefined` ולכן ההגנה הקיימת מדלגת עליהם. זו הסרת הטריגר.
  2. **ריקון דורש אישור.** שדה שרוקן בעוד שבאחסון יש ערך → `showConfirm`. מחיקה מכוונת נשמרה.
  3. **אכלוס ללא תנאי** (למעט שדה בפוקוס, כדי לא למחוק הקלדה) — אין יותר מצב ריק סמוי.
- **לקח כללי:** ‏`!== undefined` אינה הגנה על סוד. ערך ריק שמגיע מ-DOM הוא מצב לגיטימי ותכוף,
  ויש להבחין בין "לא נמסר" ל"נמסר ריק" **בשכבת הקריאה מה-UI**, לא רק בשכבת האחסון.
- **נלווה:** שחזור גיבוי מלא מציג כעת בחירה **ענן מול קובץ** עם שני התאריכים
  (`_afterFullRestore` + `cloudConfigUpdatedAt`), כי הקובץ נושא גם מפתחות וגם דאטה והענן עשוי
  להיות עדכני יותר. ‏`sendBackupNow` קיבל טוסט + נעילת in-flight (בלי פידבק הוא נראה כמו no-op
  והמשתמש לחץ שוב → מייל כפול). פרומפט המאמן עודכן: מקור התזונה הוא יומן המזון המובנה, לא MFP.
- **מגבלה מודעת בגיבוי המלא:** הוא מכסה localStorage בלבד. **רקעים מותאמים אישית (IndexedDB
  `gympro-bg`) לא נכללים** — מפתח הבחירה משוחזר אך מצביע ל-blob שאינו קיים, ו-`resolveSrc` נופל
  לברירות המחדל. תמונות התקדמות בסדר (Drive = מקור האמת).

## Carry-forward לוויטלים חסרים — הרחבה לכל המדדים + תצוגה (v17.92)

- **מה השתנה:** `_carriedRHR` הוחלף ב-`_carriedVital(nights, idx, key)` גנרי. טווחים ב-
  `VITAL_CARRY_MAX = { rhr: 3, hrv: 2, respRate: 2 }`. גם **הציון** (`computeReadiness`) וגם
  **התצוגה** (כרטיסי HRV/דופק/נשימה ב-`renderSleepView`) משתמשים בערך הנגרר.
- **שינוי מדיניות מכוון:** ההחלטה הקודמת "התצוגה מציגה את האמת (חסר) — רק הציון גורר" הוחלפה
  בגישה "אף פעם לא ריק אם יש נתון זמין". הכרטיס מציג את הערך הנגרר עם תיוג עדין "מלפני יום/N ימים"
  (`.sl-carried`) — שקוף לגמרי שזה לא של הלילה עצמו, אבל למשתמש תמיד קריאה אינדיקטיבית.
- **הבטיחות נשמרה:** הערך הנגרר עדיין **לא נשמר** ו**לא נכנס ל-baseline** (אותה ארכיטקטורה כמו
  ב-v17.76 — `_recoveryBaseline` קורא מהמערך המאוחסן בלבד). אפס זיהום של הנורמה האישית.
- **למה גם ל-HRV/נשימה:** אם RHR חסר והציון גרר אותו, אבל HRV חסר וקיבל "—" — נותרת אסימטריה.
  ההרחבה מבטיחה שכל שלושת הוויטלים המרכזיים ("HRV/RHR/נשימה") מקבלים אותה טיפול. HRV/נשימה
  בטווח קצר יותר (2 לילות) כי הם משתנים יותר מיום ליום.
- **`_carriedRHR`** נשאר כ-wrapper לתאימות בלבד (אין קוראים חיצוניים).

## RHR חסר בבוקר — carry-forward + החולשה שנשארה פתוחה (v17.76)

- **הבעיה (נמדדה 22.7.2026):** `restingHeartRate` (משקל 0.20 בציון ההתאוששות) מחושב ע"י Apple
  כמצרף יומי ומתעדכן רק סביב **12:00** — אחרי סנכרון הבוקר (~07:08). לכן הוא **חסר בבוקר**,
  והציון חושב מ-3 מדדים בלבד (HRV/שינה/נשימה). המשקל האפקטיבי של **HRV קפץ מ-0.35 ל-0.48** —
  כלומר המדד הרועש ביותר (SDNN צרכני, דגימות ספורדיות) הפך לקובע כמעט מחצית מהציון.
- **הפתרון שנבחר — החלופה הזולה:** `_carriedRHR(nights, idx)` ב-`bodylog-logic.js`. כשה-RHR
  של הלילה חסר/לא-תקין, גוררים את הערך התקין האחרון עד `RHR_CARRY_MAX_GAP=3` לילות אחורה, **רק
  לחישוב הנקודה ב-z של `computeReadiness`**. בטוח כי הערך הנגרר הוא אותו מצרף יומי של Apple —
  **אותה סקאלה בדיוק**.
- **למה זה נקי מבחינת baseline:** הערך הנגרר **אינו נשמר** ב-`gympro_sleep_daily` ו**אינו נכנס
  ל-`_recoveryBaseline`** (שקורא רק מהמערך המאוחסן). אפס זיהום של הנורמה האישית. גם ה"—" בכרטיסי
  התצוגה וב-`_buildSleepAIContext` נשאר כן — התצוגה מציגה את האמת (חסר), רק הציון משתמש בערך הנגרר.
- **מגבלה מודעת:** אירוע חד (יום צום) משפיע על הציון של **מחר**, לא של היום. זניח כי הכלי נקרא
  כמגמה, לא כנקודה יומית.

### ⚠️ החולשה שנשארה (RHR לילי מחושב — נדחה במודע, לא נזנח)

הפתרון הזול פותר **תזמון**, לא **איכות**. RHR של Apple הוא מצרף יומי **מזוהם משעות ערות** (עמידה,
מדרגות, קפאין, סטרס). Whoop/Oura משתמשים ב-**RHR לילי** מדגימות דופק גולמיות בחלון השינה — נקי
יותר ב-3-8 bpm. שדרוג לזה נבחן ונדחה בגלל **שני חסמים שהתגלו בקוד**, שחייבים להיפתר לפני ניסיון:

1. **חלון השינה לא קיים בצינור.** המודל שומר רק *משכים* (`asleepMin/deepMin/…`) — **אין
   `sleepStart`/`sleepEnd` timestamps**. בלי חלון אי-אפשר לסנן דגימות דופק לשינה. חייב לייצא
   Sleep Analysis עם חותמות זמן ולהרחיב את `_parseHAE`.
2. **מגבלות `PropertiesService` בגשר.** ~50-90 דגימות/לילה × 120 יום מפוצצות את פורמט האחסון
   הקומפקטי (`[...]` ליום, ~9KB/property). חובה **לחשב בגשר ולזרוק את הגלם** — לא לאחסן דגימות,
   ולא להעביר אותן ב-JSONP. כלומר החישוב **חייב לרוץ בגשר**, לא באפליקציה.

בנוסף: RHR לילי בסקאלה נמוכה יותר → **baseline נפרד** (28 יום building) — אסור לערבב עם היסטוריית
ה-RHR היומי. מורכבות כוללת: בינונית-גבוהה, מרוכזת ב-`_parseHAE` + נגיעות ב-`computeReadiness`/תצוגה.
**מסמך היתכנות מלא נדון ב-24.7.2026.** אם/כשנחזור לזה — לפתוח כמהלך נפרד, קודם לסגור את פער חלון-השינה.

## תזמון HAE: Shortcuts כמתזמן + שני כשלים שקטים בגשר (גשר+דוקס — ללא שינוי גרסת app)

- **תסמין 1 — "צריך לחכות או לדחוף ידנית בבוקר":** האוטומציות הפנימיות של HAE
  (Sync Interval) רצות על **Background App Refresh** — iOS מחליט מתי, לפי סוללה/שימוש/משאבים.
  בנוסף **Apple חוסמת קריאת HealthKit כשהמכשיר נעול**, כך שייצוא לא יכול לרוץ בזמן השינה.
  **אין הגדרה ב-HAE שתיתן שעה מדויקת.** הפתרון: פעולת ה-Shortcuts **Run Automation** של HAE
  (מריצה אוטומציה קיימת מיידית), תלויה באוטומציית **"כיבוי שעון מעורר"** + 09:00 (רשת ביטחון)
  + 12:30 (השלמת RHR — בטוח כי `mergeSleepDays` הוא fill-only לוויטלים). מתועד ב-`docs/hae-automation-recipe.md`.
- **תסמין 2 — "היה ייצוא אבל ה-PWA לא התעדכן":** שני כשלים **שקטים** ב-`_parseHAE`, שניהם תוקנו:
  1. **⭐ הסיבה בפועל — `Export Version: v2` אינו מכיל `date` כלל.** אומת מול הגדרות המשתמש
     (Summarize Data היה **דלוק** כל הזמן — ההשערה הראשונית על Aggregate הייתה שגויה).
     שורת `sleep_analysis` ב-v2: `startDate, endDate, totalSleep, asleep, core, deep, rem,
     sleepStart, sleepEnd, inBed, inBedStart, inBedEnd` — **בלי `date`**. `_isoDate(r.date)`
     החזיר null ו**כל השורות נזרקו בשקט** → `NO_DATA` גנרי שנראה כמו "אין נתונים".
  2. **ייחוס תאריך שגוי (v1).** `date` של HAE = היום שבו הלילה **התחיל**; האפליקציה מייחסת
     שינה ל**תאריך הבוקר**. לילה שהתחיל ב-23:30 נשמר על אתמול. כעת שורות שינה מיוחסות לפי
     **`sleepEnd`** (רגע היקיצה), עם נפילה ל-`date`/`endDate`/`startDate` — נכון לשתי הגרסאות.
  3. **זיהוי גלם לפי טוטלים, לא לפי `qty`.** ב-v1 הלא-מצטבר כל מקטע שלב מגיע כ-
     `{startDate,endDate,qty,value}` — `qty` קיים, ולכן בדיקת "מצטבר" שנשענה עליו הייתה
     מחזירה שורות אלה למסלול השקט. כעת: שורת שינה בלי `core/deep/rem/totalSleep/asleep`
     נחשבת גלם → `HAE_NOT_AGGREGATED` + hint. **לא מסכמים גלם בכוונה** (מקורות חופפים).
- **מלכודת שם:** ‏`apple_sleeping_wrist_temperature` מכיל את המחרוזת `sleep` — זיהוי שורת
  שינה מחייב גם `name.indexOf('temp') === -1`, אחרת מדד qty רגיל נבלע כשורת שינה.
- **החלטה:** התזונה **לא** עוברת ל-HAE — יומן המזון המובנה באפליקציה הוא מקור האמת.

## מקור נתוני שינה: Health Auto Export במקום קיצור iOS (גשר — ללא שינוי גרסת app)

- **הבעיה שהתגלתה:** הקיצור (Sleep Sync) סיכם דגימות שינה **גולמיות** מ-HealthKit דרך
  `Find Health Samples`. ל-HealthKit יש **מספר מקורות** לשינה (כמה "Apple Watch" מהיסטוריית
  צימודים + iPhone), עם דגימות חופפות/כפולות. אפליקציית Health מציגה מקור **מעובד/מנוקה** אחד;
  הקיצור סוכם את הכל גולמי → מספרים שגויים (Deep 60 במקום 29, Core 250 במקום 303, זמן שינה
  7:00 במקום 7:40). **זו מגבלה של Shortcuts, לא באג בלוגיקה** — אין דרך דרך Shortcuts לקבל את
  המספר המעובד.
- **הוכחה:** בדיקת דיבאג בקיצור (Repeat על דגימות Deep + Source/Date/Duration) הראתה 5 מקטעי
  Deep במקום 3, כולל דגימות זרות עם משך זהה — טביעת אצבע של מקור כפול.
- **הפתרון:** **Health Auto Export (REST API)**. מייצא טוטלים יומיים **מעובדים** (Aggregate/
  "Summarize Data") שתואמים לאפליקציית Health **לדקה** (נמדד: 7:39 מול 7:40, שלבים זהים).
- **אינטגרציה:** ‏`health-nutrition-bridge.gs` מזהה פורמט HAE (`{data:{metrics:[…]}}`) ב-`_parseHAE`,
  ממזג מדדים לפי תאריך (שינה + RHR/HRV/נשימה/טמפ'), ממיר שעות→דקות לפי שדה `units`, ושומר באותו
  פורמט שהאפליקציה כבר קוראת. **אפס שינוי בלוגיקת האפליקציה** — `mergeSleepDays` עובד כמו שהוא.
- **מלכודות:** (1) ה-token של HAE ב-**URL** (`?token=…`), כי גוף ה-JSON לא מכיל token.
  (2) פריסת Apps Script לא מתעדכנת עד **New version** — עריכה לבד לא מספיקה.
  (3) טמפ' שורש כף היד מגיעה **מוחלטת** (~35°C); האפליקציה גוזרת סטייה מ-baseline אישי — לוודא
  ש-`_validVital('wristTempDev')` לא דוחה ערך מוחלט כשמגיע לילה עם טמפ'.
- **דגל דיבאג בגשר:** `DEBUG_RAW` — כשדלוק, שומר את ה-POST הגולמי האחרון לשליפה דרך `?raw=1`
  (לאימות פורמט מקור חדש, בלי צד שלישי/מייל). ברירת מחדל כבוי.

## בורר רקעים — גריד (v17.34)

- הדפדוף האחד-אחד (`cycleBg` + אינדקס `ix` פר-slot) **הוסר**; במצב עריכת רקעים הכפתור הצף
  "בחר רקע" פותח `bg-picker-sheet` — גריד 3 עמודות: "שלי" (custom, ראשונים, עם אריח "העלה רקע"
  שמקצר ל-manager) ואז "מובנים". הקשה = החלה מיידית על ה-slot הפעיל + סגירה. הנבחר מסומן
  בטבעת accent + check. הבחירה עוברת דרך `pickerItems` (אינדקס, לא מחרוזת ב-onclick — נגד escaping).
- זרימת שמור-וסיום לא השתנתה.

## רקעים מותאמים אישית (v17.33)

- **החלטה:** רקעים שהמשתמש מעלה נשמרים ב-**IndexedDB מקומי** (‏DB ‏`gympro-bg`, ‏store ‏`bgs`,
  ‏`{id, name, blob, ts}`) — לא בריפו (public + מנפח את payload העדכון) ולא ב-Firestore (מחסום 1MB).
  פר-מכשיר במודע; אין גיבוי — אובדן רקע = העלאה מחדש. הבחירה איזה רקע לאיזה slot כן מגובה (localStorage).
- **אינטגרציה:** הכל בתוך ה-IIFE של הרקעים ב-`index.html`. בחירה נשמרת כ-`custom:<id>` בתוך
  `gympro-bg-choices-v2`; ‏`resolveSrc` ממיר ל-objectURL עם נפילה ל-DEFAULTS אם הרקע נמחק/אבד.
  ‏`cycleBg` רץ על `LIB.concat(customLib)`. מחיקת רקע מנקה את הבחירות שמצביעות עליו.
- **UI:** ‏sheet "רקעים מותאמים אישית" בהגדרות (תצוגה) — העלאה עם כיווץ ל-1600px/JPEG q0.8,
  תצוגה מקדימה שמחילה בדיוק את הטריטמנט של `.screen-bg` (grayscale+opacity 0.28+mask), שם, מחיקה.
- **מלכודת:** הטעינה מ-IDB אסינכרונית — ‏slots עם `custom:` מציגים ברירת מחדל לרגע עד שה-blobs
  נטענים ואז `applyAll` חוזר.

## ארגון מסכים מחדש (v17.30)

- **סדר טאבים חדש:** בית | Composition | אנליטיקה | ארכיון. שינוי סדר ב-tab-bar מחייב סנכרון
  `_TAB_SWIPE_ORDER` + `_TAB_SWIPE_SCREENS` ב-`workout-core.js` (ההחלקה בין טאבים לפי סדר DOM).
- **הארכיון הוא בית ההיסטוריה (החלטה):** שלושה תתי-מסכים — אימונים / שקילות / תזונה
  (`state.archiveSubTab`, `setArchiveTab` ב-`archive-logic.js`). רשימות היסטוריית שקילות/תזונה
  הוסרו מ-Composition; ה-ids (`bodylog-list`, `bl-nutrition-list`) נשמרו ועברו לארכיון, כך
  שהרנדררים ב-`bodylog-logic.js` לא השתנו. `renderBodyLog` עדיין מרנדר אותן (מרחוק) — כך כל
  שינוי דאטה שומר את הארכיון טרי. צ'יפי טווח לרשימות: `_arRange`/`setArchiveListRange`.
- **לוח שנה מאוחד:** `renderCalendar` קורא גם `getBodyLog`+`getNutritionDaily`; סמני מיקרו
  (accent=שקילה, אפור=תזונה) מתחת לנקודות האימון הצבעוניות (שנשמרו). `openDayDrawer` מקבל
  ארגומנט רביעי `extras` ומציג הכל; יום עם דאטה לא-אימונית = `.cal-day-data` (לחיץ).
- **מלכודת:** בורר הרקעים (IIFE ב-`index.html`) עוטף את `switchArchiveView` ומזהה מצב לוח-שנה
  לפי `btn-view-calendar.classList.contains('active')` — ה-id והתנהגות ה-active חייבים להישמר.
- לוח-השנה כבר לא segment: כפתור אייקון (`.archive-cal-btn`) שמחליף תצוגה גלובלית מעל תתי-המסכים.

## תמונות התקדמות + ניתוח AI משורשר (v17.24–17.28)

**ארכיטקטורת אחסון היברידית (החלטה):** תמונות גוף דחוסות (~1600px JPEG q0.82, ‏150-400KB)
לעולם **לא** ב-Firestore (מחסום 1MB/doc — נלחמנו בו ב-v17.15) ולא רק מקומית (iOS מפנה
אחסון PWA). לכן: **IndexedDB** (`gympro-photos`, stores: photos+thumbs) = cache מקומי
וגלריה מיידית; **Google Drive** דרך גשר `docs/photo-bridge.gs` = מקור אמת (תיקייה פרטית
"GymPro Progress Photos", קובץ `YYYY-MM-DD.jpg` ליום); על ה-config המסונכרן רוכבים רק
`photoIndex` (קל: date/driveId/bytes) ו-`photoTrend` (זיכרון ה-AI, entries≤30).
זו התבנית הראשונה של IndexedDB בריפו — עטופה ב-`photos-logic.js` (`_ppIdb*`).

**עקרונות דיוק AI (החלטות מוצר, לא רק פרומפט):**
- אין השוואה במרווח < 10 ימים — נאכף בקוד (`_PP_MIN_COMPARE_DAYS`), גם בניתוח ידני.
- שער "בר-השוואה" (0-10) לפני השוואה; מתחת ל-5 המודל מחזיר flags בלבד (פידבק צילום).
- עיגון בדאטה אמיתית: משקל despiked (שימוש חוזר ב-`_despikeWeight`), ממוצעי תזונה.
- אסור למודל להמציא מספרים (אחוז שומן/היקפים) — וה-UI לא מציג כאלה.
- שרשור: עד 3 תמונות (נוכחית/השוואה/עוגן=הראשונה) + `aiNotes` מצטבר + 5 entries אחרונים.

**מלכודת mirror שנתפסה בביצוע:** פריים גולמי מ-getUserMedia **אינו** mirrored גם במצלמה
קדמית — ה-mirror הוא קונבנציית תצוגה בלבד. לכן הלכידה לא מתהפכת לעולם; רק התצוגה החיה
וה-Ghost Overlay מקבלים `scaleX(-1)` ב-CSS (`.pp-cam.mirror`). היפוך בלכידה (כפי שתוכנן
במקור) היה יוצר דווקא חוסר עקביות בין קדמית לאחורית.

**פרטיות:** תמונות גוף = הדאטה הרגיש ביותר. לעולם לא דרך גשר הווידג'ט; אזהרת Gemini
free tier (גוגל רשאית להשתמש בקלט) מוצגת בהגדרות. טריאדת הגשר ב-`_connectionKeys()`.

## ארוחות שמורות ביומן המזון (v17.23)

**החלטת מודל:** ארוחה שמורה היא פריט רגיל ב-`KEY_FOOD_DB` — `source:'meal'`, `id:'meal:...'`,
`baseUnit:'unit'` עם טריק ה-per100 ×100 (per100 = ערכי המנה השלמה ×100), ובנוסף שדה
`components[]` ({name, grams, per100, baseUnit}). כך חיפוש/אחרונים/מועדפים/סנכרון/גיבוי/prune
עובדים בלי שינוי — כל קוד שלא מכיר `components` רואה מזון "נספר" תקין ששווה ארוחה שלמה.

**זיהוי:** `components && components.length` הוא הדגל בכל הצמתים (בחירה, עיפרון, רינדור) —
לא `source === 'meal'` — כדי לא לשבור אם ייווצרו פריטים עם מרכיבים ממקור אחר.

**התנהגויות מפתח:**
- בחירת ארוחה שמורה בכל רשימה → Meal Builder ממולא (מצב 'log'); שינויים ברגע התיעוד לא נכתבים חזרה לארוחה השמורה. תיעוד מעדכן `bumpFoodUsage` דרך `_fdMealSrcId`.
- בחירה בעת `_fdCompPickMode` (מרכיב למנה אחרת) → מרכיביה נפרסים לרשימת המרכיבים.
- ל-Meal Builder נוסף מצב שלישי `_fdMealMode='saved'` (עריכת מאגר בלי כתיבה ליומן): בלי chips/שעה/טוגל; שמירה → `_fdUpsertSavedMeal`; מחיקה → מהמאגר. **במצב saved אסור לגעת ב-`_fdMeal`** — פתיחה מהעיפרון בטאב מותאמים לא צריכה לשבש את הארוחה שנבחרה בשיט ההוספה שמאחור.
- טוגל "שמור גם במותאמים" (מצב log) יוצר עותק חדש במאגר בעת שמירה ליומן — גם בעריכת רשומה קיימת (שמירה רטרואקטיבית).
- כפתור "שמור" בכותרת ארוחה ביומן אוסף את כל רשומות הארוחה של היום לארוחה שמורה אחת: composite נפרס למרכיביו; רשומת "יחידה" מנורמלת מ-×100 (`per100/100`, grams=qty); רשומה ללא per100 נגזרת מסך הרשומה לפי משקל.

## הגנת אובדן-דאטה בסנכרון ענן (v17.15)

**הבעיה שנפתרה:** מסמך `config` ב-Firestore היה יחיד וגדל ללא גבול (בעיקר `foodLog` —
רשומה ליום, ~0.5MB לשנה). חריגה ממחסום ה-1MB/doc הייתה חוסמת את הגיבוי לנצח,
וחלק ממסלולי השמירה נכשלו בשקט (`.catch(() => {})`).

**הפתרון (5 שכבות):**
1. `foodLog` הוצא מ-config ומפוצל ל-chunks של 90 ימים (`food_log_meta` + `food_log_0/1/...`),
   כמו הארכיון. תאימות לאחור: `_applyConfigData` עדיין קורא foodLog מ-config ישן,
   ו-`_loadFoodLogSilent` (שרץ אחריו) גובר כשהמבנה החדש קיים.
2. שומר גודל מקדים: `_estimateDocSize` לפני כתיבת config/ai_history; מעל 800KB נרשם
   `<store>Warn` ומוצגת אזהרה (באנר + הגדרות) — חודשים לפני המחסום.
3. `KEY_SYNC_STATUS` הורחב לכל 4 המסלולים (`archive/config/raw/ai` + `Ok/At/Err/Warn`).
4. סיווג שגיאות: `_isDocTooLarge` מזהה invalid-argument של גודל → הודעה מפורשת
   ("המסמך חורג ממגבלת Firestore") במקום "בדוק חיבור" מטעה.
5. אפס מסלולים שקטים: כל קריאות הרקע מדווחות דרך `notifyCloudSaveFailure`
   (מדלג בכוונה כשלא מוגדר/לא חמוש — דילוג איננו כשל).

**מלכודת:** מכשיר עם גרסת אפליקציה ישנה (לפני 17.15) שמשחזר מהענן לא יראה את
יומן המזון החדש (הוא מחפש foodLog בתוך config) — יש לעדכן את האפליקציה קודם.

## יומן מזון — לוח שנה + יתרה שבועית (v17.08–17.09)

- **לוח שנה לבחירת יום (v17.08):** לחיצה על התאריך בכותרת היומן פותחת `#fd-cal-sheet` (bottom-sheet). החליף את הפיקר הנייטיב (`<input type=date>` נסתר + `showPicker()`) שהוסר — לא אמין ב-iOS PWA. גריד ראשון–שבת (תבנית `renderCalendar` של הארכיון), נקודת `--success` על ימים עם רשומת NUTRITION_DAILY, עתיד חסום, בחירה → `fdSetDate`. תלות: `MONTH_NAMES_HE` מ-archive-logic.js (נטען לפני food-logic.js).
- **יתרה שבועית בטבעת (v17.09):** `_fdWeeklyBalance(dateStr)` — Σ(יעד−בפועל) על השבוע הישראלי (ראשון–שבת) של היום המוצג, **בלי היום השוטף**; ימים ללא תיעוד/יעד מדולגים; היעד פר-יום מ-`getTargetsForDate` (לא היעד החי). מוצג כ-chip "שבועי ±N" בתוך `.fd-ring-center` מתחת ל"נותרו" (ירוק/אדום סמנטי). **לחיצה = טוגל תצוגה** (`fdToggleWkbal`): המספר הגדול ← נותרו־היום + היתרה, נצבע **אינדיגו `--type-free-soft` (#9F9CFF, החלטת משתמש)** — קבוע לשני הכיוונים, מסמן "תצוגה משולבת" ולא טוב/רע. הקשת עצמה נשארת של היום בלבד. הטוגל מתאפס ב-`openFoodDiary`; `_fdSkipRingAnim` מונע ריפליי של אנימציית הקשת בטוגל. הערה: `.fd-ring-center` הוא `pointer-events:none` — ה-chip מחזיר `auto`.
- **לקח תהליכי:** מוקאפים דרך Playwright שמזריק HTML/CSS לאפליקציה החיה (שרת מקומי + פונטים מקומיים) נותנים צילומי מסך אמיתיים לאישור עיצוב לפני מימוש — עדיף על מוקאפ HTML סטטי.

## TM קבוע לתרגילי MAIN (v16.78)

מטרה: לחסוך הזנת 1RM ידנית בתחילת כל תרגיל-מיין — TM נקבע פעם אחת בהגדרות (סמנטיקת 5/3/1: קבוע, לא מחושב מחדש כל אימון).

- **מודל:** `KEY_EXERCISE_TM` ב-storage.js — מפה `{exName: tmVal}`, `getExerciseTM`/`saveExerciseTM`/`getAllExerciseTMs`. ערך ריק = מחיקת מפתח (חוזר לזרימת 1RM הידנית). **לא** ב-`_connectionKeys()` (דאטה, לא סוד) ו**לא** בסנכרון Firebase config — תואם RM/weights הקיימים שגם לא מסונכרנים.
- **UI הגדרות:** `_renderMainTMSettings()` (workout-core.js) מרנדר שדה לכל שם תרגיל-מיין שנאסף מ-**כל** התוכניות (`_getAllMainExerciseNames`, לא רק התוכנית הנוכחית) ב-`#main-tm-settings-list` (הגדרות → מאמן). נקרא מ-`openSettings()`.
- **דילוג אוטומטי:** `confirmExercise()` — אם `getExerciseTM(exName) != null`, קורא ל-`applyRMAndStart(tm)` במקום `setupCalculatedEx()` (שפותח את `ui-1rm`). `applyRMAndStart` הוא ה-refactor המשותף שחולץ מ-`save1RM()` — שתי הזרימות (TM מוגדר / הזנה ידנית) מובילות לאותו חישוב סטים מאחוזי RM.
- **תצוגה — עדיפות session over settings:** `_displayTM(exName)` = `state.rmUsed[exName]` (אם בוצע בסשן הנוכחי) אחרת `getExerciseTM`. כך אם המשתמש משנה TM בהגדרות אחרי שכבר רשם סטים, הסיכום/הלוג ממשיכים להציג את הערך שבו **בפועל** בוצע התרגיל, לא את הערך העדכני בהגדרות.
- **נקודות תצוגה (חובה synchronized — באג עתידי אם רק חלק יתעדכן):** סיכום אימון (`buildSummaryUI`, badge בכרטיס), תג Main בארכיון (`_saveToArchive`, `(Main, TM: Xkg)`), ושלושת חלונות ה-Quick Menu — תרגילים (`openCurrentPlanSheet`), יומן (`openSessionLog`), היסטוריה (`openHistoryDrawer`). שלושתם משתמשים ב-`_isMainExInCurrentWorkout(exName)` (לא `item.isMain` ישיר — `openSessionLog`/`openHistoryDrawer` לא מחזיקים את ה-plan item, רק שם).
- **CSS:** `.plan-tm-badge`/`.card-tm-badge`/`.slog-tm-badge` — pill עקבי (`#353535`, `border-radius:9999px`), נפרד מ-`.plan-main-badge` (accent) כדי לא להתחרות חזותית. RTL: `margin-inline-start` (לא `margin-right`) — הבדג' מגיע אחרי הטקסט במקור ה-HTML ומוצג משמאלו.

## יומן מזון מובנה (Food Diary) — v16.40

תיעוד תזונה ישירות באפליקציה (חלופה/השלמה ל-MFP), מבוסס **Open Food Facts** (חינמי, ללא מפתח, CORS ישיר).

**עיקרון אינטגרציה (קריטי):** `KEY_NUTRITION_DAILY` הוא מקור-האמת היחיד לכל הצינור (כרטיס בית, Composition,
TDEE, AI). היומן הפנימי שומר רשומות per-food ב-`KEY_FOOD_LOG` ({date:[entry]}), ו-`recomputeNutritionDay(date)`
מסכם את היום → upsert ל-NUTRITION_DAILY עם `src:'app'`. כך כל הצרכנים הקיימים נדלקים **ללא שינוי בהם**.

**עדיפות בהתנגשות (החלטת משתמש):** `mfp > app > health`. `recomputeNutritionDay` **לא דורס** יום שמקורו MFP
(src ריק/undefined). לכן לוגינג פנימי אפקטיבי בעיקר ל"היום" ולימים שטרם יובאו מ-MFP; ייבוא MFP מאוחר ידרוס את
הסיכום היומי (רשומות היומן עצמן נשמרות תמיד).

**מודל:** `KEY_FOOD_LOG` (יומן), `KEY_FOOD_DB` (קאש OFF + מותאמים + מועדפים/אחרונים). יעדי מאקרו ב-prefs
(`proteinTarget/carbsTarget/fatTarget`), שמות ארוחות חופשיים ב-`mealLabels`.

**יעדים אפקטיביים-מתאריך (v16.91):** שינוי יעד קלוריות/מאקרו חל **מהיום והלאה בלבד** — ימי עבר ביומן
ובכרטיס הבית מציגים את היעד שהיה בתוקף בהם. `KEY_TARGET_HISTORY` (storage.js): `[{date,kcal,p,c,f}]`;
`recordTargetChange(prev)` נקרא מ-`saveKcalTarget`/`saveMacroTarget` (workout-core) עם snapshot מ-**לפני**
המוטציה — בפעם הראשונה מקבע את הערך הישן כרשומת `2000-01-01` ("מאז ומתמיד"), ואז upsert של היום (כמה
שינויים באותו יום — האחרון קובע; שינוי ללא הבדל אפקטיבי לא נרשם). `getTargetsForDate(date)`: היום ואילך →
תמיד ה-prefs החיים (עמיד לייבוא/שחזור שעקף את הלוג); עבר → הרשומה האחרונה ≤ התאריך; לוג ריק → prefs
(תאימות לאחור). צרכנים: `_fdSummaryHTML` (פרמטר `tg`), כרטיס הבית (`_homeTodayRenderNutrition` — לפי
`latest.date`). ה-prefs הגלובליים נשארים מקור האמת ל"עכשיו" (הגדרות/AI). מסונכרן: config ענן + ייצוא/ייבוא config.
**עורך "יעדים לפי תאריך" (v16.92):** כניסה דיסקרטית (אייקון 🕘 `.nutri-th-btn` ליד יעד הקלוריות בהגדרות) →
`#target-history-sheet`. עריכת **עבר בלבד** (< היום, נאכף ב-`thSaveEntry` + `max` על בורר התאריך) —
"מהיום ואילך" נשאר בשדות הרגילים, כדי לא לשבור את הכלל `getTargetsForDate(היום+) = prefs`.
`upsertTargetEntry`/`deleteTargetEntry` ב-storage.js; שמירה/מחיקה מסנכרנות ענן ומרעננות את כרטיס הבית.
זה גם הפתרון לתיקון רטרואקטיבי ידני — המשתמש מזין את היעדים הישנים + תאריך המעבר, במקום מיגרציה בקוד.
**חלון חסד + מחשבון (v16.94):** `recordTargetChange` קיבל את סמנטיקת `setNutritionalState` —
`TARGET_GRACE_MS` (10 דק'): שינוי בתוך החלון דורס את רשומת-היום (ts נשמר), חזרה לערכים הקודמים
מקפלת אותה לגמרי (כולל ניקוי seed מיותם — לוג חוזר לריק אחרי בדיקת-כפתורים). החסד חל רק על רשומת
היום מהזרימה הראשית — רשומת עבר ידנית אחרונה לא נדרסת. רשומות נושאות `ts`. בעורך: מחשבון
מאקרו→קלוריות זהה לשדות הראשיים (`_thKcalManual`, תג מחושב/ידני, מחיקת קלוריות = חזרה לאוטומטי;
בעריכת רשומה המצב משוחזר בהשוואת kcal לחישוב).

**UI:** אין טאב קבוע — Overlay מסך-מלא (`#food-diary`) הנפתח מכפתור "יומן תזונה" בכרטיס הבית (מתחת ל-LIVE)
ובמסך Composition. ניווט ימים: חיצים + החלקה + בורר תאריך. "+" לכל ארוחה (סגנון MFP). עורך מנה/כמות/שעה/ארוחה.
ברקוד: **פענוח מקומי** (`_fdDecodeBarcode` עם `BarcodeDetector`) על הצילום לפני AI; כשל → Gemini לקריאת תווית.
**סריקה חיה** (`fdLiveScanStart`, overlay `#fd-live`, z-index 2000): `BarcodeDetector` בלולאת rAF, או ZXing
(`vendor/zxing.min.js`, טעינה עצלה) ב-iOS. ל-OFF דרך `lookupBarcode`. מנת אוכל/תווית עדיין דרך Gemini.

**לקחים:** (1) ids של מוצרי OFF ללא ברקוד מנוקים לתווים בטוחים ל-onclick. (2) חיפוש תלוי-רשת; אחרונים/מועדפים/
מותאמים עובדים offline (קאש ב-KEY_FOOD_DB). (2.5) **אחרונים/מועדפים — עדיפות לארוחה הנוכחית (v16.45):** `getFoodDb()`
שומר `mealUse: {meal: {count, lastUsed}}` שמתעדכן ב-`bumpFoodUsage(id, meal)` בכל שמירת מנה בפועל (`fdSavePortion`).
`StorageManager._mealSort(meal)` ממיין כל רשימת מזון כך שמוצרים שתועדו בארוחה מהסוג הנוכחי מובילים (לפי `lastUsed` שלהם
בארוחה הזו), ואחריהם השאר לפי `lastUsed` גלובלי. `recentFoods(n,meal)`/`favoriteFoods(meal)` משתמשים בזה ישירות —
`fdRenderTab` מעביר את `_fdMeal` הנוכחי. (3) z-index: overlay=300, ה-bottom-sheets (1000/1001) מעליו בכוונה.
(4) פענוח ברקוד = מקומי ומיידי, לא AI — AI שמור לקריאת תווית/הערכת מנה בלבד. (4.5) **חיפוש — עדיפות למתועד (v16.82):** `fdDoSearch` מפריד את התוצאות המקומיות התואמות ל-`used` (יש `lastUsed`) ומציב אותן תמיד בראש. **לקח:** לפני התיקון, ה-`merged` הסופי (אחרי שהרשת עונה) נבנה מ-`basics+foods` בלבד — כל התאמה מקומית (כולל מתועדת) שלא חזרה גם מהרשת **נעלמה** מהתוצאות אחרי שתשובת הרשת הגיעה; זו הייתה הסיבה שמוצרים מוכרים "ברחו" מהראש. (4.6) **מנוע דירוג חיפוש (v16.88):** `_fdScore(text,q)` — ציון רלוונטיות עם סמנטיקת AND (ריבוי מילים דורש את כולן) וגבולות-מילה ("חלב" מעדיף "חלב 3%" על "חלבון ביצה"; מילה-שלמה 85+ > תחילית 75 > substring 50). ≥50 = התאמה מלאה; 0<ציון<50 = חלקית, מוצגת רק מאחורי "הצג עוד" (`fdShowMoreResults`, קאפ `_FD_SHOW_LIMIT=12`); 0 = מוסתר (מסנן גם את רעש ה-full-text של צמ"ת שתואם שדות צדדיים). הציון כולל את המותג (`_fdFoodScore`). **סדר השכבות (החלטת משתמש):** תועדו-בארוחה-הנוכחית → מועדפים → תועדו-אי-פעם (`StorageManager.sortUsedForSearch`) → בסיסיים → רשת מדורגת (ציון, שובר שוויון צמ"ת>OFF>USDA ואורך שם) → cache לא-מתועד. שכבת "המזונות שלך" כוללת גם מועדפים בלי `lastUsed`. **OFF דו-שלבי:** `_offQuery(q, israelOnly)` — קודם מוצרים מתויגי-ישראל ממוינים לפי פופולריות (`sort_by=unique_scans_n`; במודרני — `countries_tags:"en:israel"` בתוך q), ואם <5 תוצאות — השלמה גלובלית. **קאש רק בבחירה:** הוסרה השמירה הגורפת של תוצאות רשת ל-KEY_FOOD_DB (זיהום מצטבר שסונכרן גם לענן); מוצר נשמר רק ב-`fdSelectFoodById`/ברקוד/AI. ניקוי חד-פעמי `pruneUnusedFoodCache` (storage.js, שמרני — רק off/usda/tzameret בלי שום אות שימוש) רץ ב-`openFoodDiary` עם דגל `gympro_fd_cache_pruned`. (5) USDA אנגלי-בלבד: `_fdTranslateForUsda`
ממפה עברית→אנגלית (`_FD_HE_EN`) לפתיחת חומרי גלם גנריים (דורש מפתח USDA). (6) `_fdTokenMatch` — התאמת חיפוש
מבוססת-טוקן+נרמול ניקוד, מחליפה `indexOf` נאיבי. (7) ZXing מ-`vendor/` — runtime-cache ב-SW, לא pre-cache (336KB). (8) **Gemini כ-fallback תזונתי** (`_fdAiFood`/`fdAiLookup`):
שורת "הערכת AI" יזומה (`_fdAppendAiAction`) קבועה מתחת לתוצאות, ואוטומטית ב-0 תוצאות. מזון גנרי = ערכים סטנדרטיים אמינים,
נשמר ב-DB (id `ai:`) → קאש מיידי+offline. פותר מזון טריוויאלי בעברית בלי מאגר/מילון ידני. (9) **עריכת מזון מותאם + יחידות מידה (v16.83):**
עיפרון עריכה (`fdEditCustomFood`) בכל שורת מזון עם `source==='custom'` (כל רשימות המזון — `_fdRenderFoodList` משותף לכולן) פותח את אותו טופס יצירה
(`_fdShowCustomFoodForm`, מצב משותף create/edit לפי `_fdEditCustomFoodId`) ושומר במקום דרך `upsertFoodToDb` (merge-by-id קיים — משמר `useCount`/`favorite`/`mealUse`
ללא שינוי ב-storage.js). רשומות שתועדו בעבר (`KEY_FOOD_LOG`) שומרות snapshot קפוא של `per100` באותו רגע — עריכה מאוחרת **לא** משנה היסטוריה (`recomputeNutritionDay`
מסכם מ-`entry.kcal/p/c/f` המוכנים מראש, לא מ-`per100`). **יחידת מידה:** `food.baseUnit`/`entry.baseUnit` ∈ `'g'|'ml'|'unit'` (ברירת מחדל `'g'`, לא קיים במזון
חיצוני/ישן — שומר תאימות מלאה). **טריק יחידה בודדת (×100):** המודל הפנימי מחושב כולו כ-`per100 * grams/100`; כדי לבטא "ערך ליחידה אחת" (לדוגמה ביצה) בלי
לשנות את הנוסחה הזו, הערך המוזן מוכפל ×100 לפני האחסון ב-`per100`, וה-`servings` מוגדר ל-`[{label:'1 יחידה', grams:1}]` — כך `grams=qty` ו-`macros=realValue*qty`
מתקבלים נכון ללא כל שינוי בלוגיקת `_fdComputeGrams`/`_fdMacrosFor`. כל מקום שמציג `per100` גולמי ישירות למשתמש חויב בחילוץ ÷100 מפורש: `_fdRenderFoodList`
(שורת תוצאה), `_fdShowCustomFoodForm` (prefill בעריכה), `_fdUpdatePreview`/`_fdQtyDisplayLabel` (תצוגת כמות חיה). **Meal Builder — תוקן (v16.84):** רכיבי
ה-Meal Builder (`_fdMealComponents`) נושאים כיום `baseUnit` (מועבר מ-`_fdAddComponentFromFood`, משוכפל ב-`_fdOpenMealBuilder`/`fdEditEntry`, ונשמר בסנאפשוט
הקפוא ב-`fdSaveMeal`). מזון מסוג `'unit'` מנורמל ל"ערך אמיתי ליחידה" (÷100) כבר בכניסה לרכיב — לא נשמר טריק ×100 בתוך ה-Meal Builder. `_fdRenderComponents`/
`_fdMealRecalc` מציגים תווית "ליחידה:"/"יחידות" ומחשבים `kcal = perUnit * qty` (לא `/100`) עבור `baseUnit==='unit'`; שאר היחידות (`g`/`ml`) ללא שינוי.
**מחיקת מזון מותאם (v16.84):** `StorageManager.deleteFoodFromDb(id)` (לצד `upsertFoodToDb`) מסנן מ-`KEY_FOOD_DB`; אין צורך בניקוי הפניות — רשומות יומן עצמאיות (snapshot קפוא).
כפתור מחיקה (`fdDeleteCustomFood`, `.fd-del-btn`) מופיע רק במצב עריכה בתוך `_fdShowCustomFoodForm`, עם אישור דרך `showConfirm` (בלתי-הפיך, בשונה ממחיקת רשומת יומן בודדת).
**קיצור דרך לטאב מותאמים (v16.84):** כפתור `.fd-manage-btn` בכותרת יומן המזון (מקום `.fd-header-spacer` הישן) → `fdOpenCustomFoodsManager()` פותח את שיט ההוספה ישר על טאב "מותאמים".
**התראת חוסר-תאימות קלוריות/מאקרו (v16.87):** `_fdKcalMismatch(kcal,p,c,f)` — בדיקת Atwater טהורה (`p*4+c*4+f*9`), ללא state/persist; מחזיר null מתחת לסף (>30 קלוריות ושני הצדדים, סטייה >15%) או יחס הסטייה. מוצג בשלוש נקודות הזנה: עורך המנה (`_fdUpdatePreview`, מכסה את כל המקורות — OFF/Gemini/מותאם/מועדפים/אחרונים כי כולם עוברים `_fdOpenPortion`), טופס מזון מותאם (`_fdCustomCheckMismatch` עם `oninput` על 4 השדות — actionable כי המשתמש הקליד את הערכים), Meal Builder לפי-מרכיב (`_fdMealRecalc`, סימן ⚠ קטן עם `title` ליד kcal כל מרכיב בנפרד). אזהרה אינפורמטיבית בלבד — לא חוסמת שמירה, לא נשמר flag חדש ב-storage (ניתן לשחזור מ-`per100`/`kcal/p/c/f` הקיימים). תקדים: `bl-tdee-warn` ב-bodylog-logic.js.
**הערה יומית (v16.85):** `KEY_NUTRITION_NOTES` (מפה `{date: text}`) **בכוונה עצמאי** מ-`KEY_NUTRITION_DAILY` — לא שדה על רשומת היום. סיבה: `recomputeNutritionDay`/`mergeHealthNutritionDays`/`applyMfpDays` עושות דריסה מלאה של רשומת היום (לא מיזוג שדות), ושדה `note` היה נמחק בשקט בכל לוג/מחיקת מזון או ייבוא MFP/Health עתידי; וביום בלי שום רשומת תזונה אין רשומה להצמיד אליה הערה בלי ליצור רשומת-דמה שמזהמת ממוצעים/streaks/heatmap שצורכים `getNutritionDaily()`. `getNutritionNote`/`setNutritionNote` ב-storage.js; UI דרך שיט משותף `#fd-portion-sheet` (`_fdShowNoteForm`/`fdSaveNote`, אותה תבנית כמו `_fdShowMealNamePrompt`) ושורת `.fd-note-row` ב-`fdRender` בין הסיכום לארוחות. ריק = מחיקת ההערה. מסונכרן לגיבוי/ענן כדאטה רגיל (לא ב-`_connectionKeys()`, תואם תקדים `KEY_EXERCISE_TM`).

---

## מסך הבית — כרטיסי "היום" (v16.05)

- הטאב "אימון" שונה ל-**"בית"**: סקשן השיאים האישיים הוחלף בשתי כרטיסיות "היום" — תזונה (ימין, LIVE badge כשהנתון מהיום) והרכב גוף (שמאל: משקל, אחוז שומן, LBM, שינוי 30 יום). לחיצה מנווטת ל-Composition עם תת-טאב מתאים (`goToComposition` קובע `_blTab` לפני `switchMainTab` — בלי `setBodyTab`, שמרנדר כפול).
- סקשן ה-PR **לא נמחק** — pref חדש `homeCard: 'today' | 'pr'` ב-analytics prefs + toggle בהגדרות (`toggleHomeCard`/`applyHomeSectionPref` ב-archive-logic.js). **מלכודת:** `getAnalyticsPrefs()` לא ממזג defaults למשתמשים קיימים — כל קריאה חייבת `prefs.homeCard || 'today'`.
- רענון הכרטיסים: בכל `switchMainTab('workout')` + אחרי כל שינוי דאטה (שקילה/מחיקה/ייבוא CSV, סנכרון Health, ייבוא MFP, איפוס תזונה) דרך `renderHomeTodayCards()`.
- **טווח חישוב TDEE ידני (v16.10):** pref `tdeeStartDate` (null = אוטומטי, 28 ימים/תחילת שלב+7). כשמוגדר — `computeTDEE` עוקף את לוגיקת השלב ומושך מהתאריך הידני. נקודות כניסה לבורר התאריך (`#tdee-range-modal`): שורת "טווח חישוב" בראש הפירוט + מספר הימים בטבלת השיטות (שתיהן `openTdeeRangeModal`). מצב "ריק" (טווח ידני שמרוקן נתונים) מציג קישור reset כדי למנוע מצב לכוד.
- **רענון Health מהבית (v16.09, ~~בוטל ב-v18.5~~):** תג ה-LIVE בכרטיס התזונה היה לחיץ — `refreshHomeNutrition()` → `syncHealthNutrition(true)` עם `stopPropagation` (שאר הכרטיס מנווט ל-Composition). התג מוצג רק כשיש נתון מהיום — ביום ללא נתונים אין נקודת רענון בבית.
- **כפתור "+" בכרטיסי היום (v18.5):** תג ה-LIVE ואייקון היומן (`menu_book`) ירדו; במקומם כפתור "+" יחיד ואחיד בפינת שני הכרטיסים — תזונה → `openFoodDiary()`, הרכב גוף → `openBodyEntryModal()`. **המניע:** שני הכרטיסים היו א-סימטריים (לתזונה שתי פעולות נסתרות, לשקילה אפס), והלחיצה על שורת הקלוריות הייתה בלתי-נראית. שורת המספר היא כעת תצוגה בלבד.
  - **מלכודות:** (1) הכרטיס עצמו הוא `<button>` — הפלוס חייב להיות `<span role="button">`, כי `<button>` מקונן אינו HTML תקין; (2) `openBodyEntryModal(date)` נקראת **ללא ארגומנט** — העברת האירוע תיקרא כתאריך ותפתח "עריכת שקילה" של רשומה לא קיימת.
  - ⚠️ **`refreshHomeNutrition()` נשארה יתומה במכוון** (`archive-logic.js`) — אין יותר נקודת כניסה למשיכת Health ידנית מהבית. **כשנפעיל מחדש את הקישור ל-MFP/Health — חובה לחווט אותה מחדש** (כפתור בכרטיס, לחיצה ארוכה, או פעולה במסך Composition). לא למחוק בינתיים.
- **יעד קלורי יומי (v16.08):** pref `kcalTarget` (null = כבוי), קלט בבנטו ה-Nutritional State (הגדרות → מאמן, `saveKcalTarget` ב-workout-core.js). כרטיס התזונה בבית מציג מתחת לצריכה את ההפרש יעד−צריכה — מספר בלבד, ירוק כשנותר / אדום כשחרגו, `direction:ltr` כדי שהמינוס יוצג נכון.

---

## החלטות ארכיטקטוריות (לא ברורות מהקוד)

- **Vanilla JS ללא Framework** — בכוונה. PWA offline-first, 0 תלויות, bundle=0.
- **SPA display-toggle** — אין routing. כל screens ב-`index.html`, ניווט ע"י `historyStack`.
- **Gemini API ישיר מ-client** — ללא backend. מפתח ב-LocalStorage. סיכון מודע, קביל ל-personal app.
- **SW cache-first + skipWaiting** — עדכון מיידי. המשתמש חייב ללחוץ "בדוק עדכון" לראות גרסה חדשה.

---

## מלכודות קריטיות

### ניווט
- `navigate(id)` — **מקור האמת** לניווט. מעדכן tab-bar, session-strip, settings/sound/reload btns.
- **הערימה היא single-instance** (v19.4.7): `navigate` מקצץ אל מופע קיים במקום לדחוף
  כפילות. אל תחזיר push עיוור — הוא היה מקור לערימה מתנפחת ולחזרה למסכים מתים.
- **`handleBackClick` נשען על ה-DOM** (`_activeScreenId`), לא על ראש הערימה, ומיישר
  את הערימה אליו (`_syncStackToActiveScreen`) לפני כל פעולה.
- **כל מסך שחוזרים אליו חייב case ב-`_refreshScreen`** — מסך חדש בלי case יוצג עם
  התוכן של הרינדור הקודם. אותו helper משמש גם את `restoreSession`.
- **"חזור" לא מוחק דאטה בלי אישור, ולא נשאר no-op.** יציאה מאימון תמיד דרך
  `_abandonWorkout()` (מנקה גם `state`, לא רק אחסון — אחרת `_persistOnTeardown`
  משחזר את הסשן שננטש).
- `NO_BACK_SCREENS` כולל גם `ui-summary` ו-`ui-ask-extra` — מסכים שאחריהם האימון
  כבר נשמר / אין אליהם מסך קודם הגיוני.
- `restoreSession()` **לא קוראת** ל-`navigate()` — חייבת לשכפל את לוגיקת ה-UI ידנית (כבר תוקן ב-55).
- `updatePlanFloatBtn()` מחפש `#ui-main .header-tools` — אם ה-HTML משתנה, לעדכן גם כאן.

### CSS / Layout
- `.screen` הוא flex container. ילדיו מתכווצים (`flex-shrink:1`) ולא מייצרים overflow. **חובה:** `.screen > * { flex-shrink: 0 }` לגלילה. (תוקן ב-55)
- יחידות: תמיד `rem`, לא `em`. `em` תלוי בהקשר ומפתיע ברכיבים מקוננים.
- RTL: ב-`flex-direction: row` — `flex-start` = ימין ויזואלית, `flex-end` = שמאל.
- `align-self: flex-start` חובה על pill buttons בתוך flex-column (אחרת נמתחים לרוחב מלא).
- `#vol-muscle-chips` — CSS ייעודי עם `flex-wrap: nowrap` + overflow-x scroll + chips קטנים (`0.75rem`). אין לשנות ל-wrap.

### Analytics
- `switchAnalyticsTab()` מחפש `#analytics-seg .seg-btn` — ה-wrapper **חייב** להכיל `id="analytics-seg"`. ללא ה-ID, שניהם מקבלים `.active` ונראים אפורים. (תוקן ב-74)
- גרף נפח לאורך זמן (`renderVolumeBarChart`): **אין להוסיף `.reverse()`** — ב-RTL האלמנט הראשון במערך מוצג מימין. archive מגיע חדש-ראשון, אז ללא reverse: חדש=ימין. (תוקן ב-74)
- `.x-axis-lbls` חייב `direction: ltr` — ה-SVG מצייר LTR פיזית, התאריכים חייבים להתאים. (תוקן ב-74)
- כרטיסיות "נפח כולל" ו-"שיא נפח" בסקירה — מוצגים בק"ג עם `toLocaleString('he-IL')`, **לא** בטון. (תוקן ב-74)
- `confirmExercise()` מזהה מסך מבוא סבב לפי `!state.currentEx` — לא לפי innerText (שביר). (תוקן ב-55)
- `deepClone(undefined)` = SyntaxError שקט. תמיד null-guard לפני `deepClone`.

### PWA / Deploy
- **התקנת SW חייבת `fetch(url, {cache:'no-store'})` + `cache.put`** (תוקן ב-15.97.1) — `cache.addAll` רגיל עובר דרך ה-HTTP cache של iOS ומקבל קבצים ישנים: נוצר cache עם שם גרסה חדש אבל תוכן ישן, כולל `version.json` ישן → "עדכון זמין" קופץ לנצח והגרסה לא מתעדכנת. ייתכן שזה גם גרם בעבר לתיקונים "שלא עבדו" — הם לא הגיעו למכשיר.
- `checkForUpdate` ממתין ל-`reg.update()` + `controllerchange` (עד 4 שניות) לפני הרענון — אחרת ה-SW הישן מגיש את הרענון והעדכון נראה "תקוע".
- push לbranch `claude/**` בלבד לא מספיק — האפליקציה מוגשת מ-`main`.
- GitHub Actions auto-merge **קורס על push שני+** לאותו branch (branch כבר קיים). **חובה: merge ידני** לאחר כל push.
- `sw.js` + `version.json` חייבים להשתנות באותו commit עם שאר הקבצים. bump גרסה ב-commit נפרד = cache ישן לטעות.

---

## ערכות צבעים (Color Themes) — v16.38→16.39

6 ערכות: `obsidian` (ברירת מחדל, ללא attribute), `bronze` (iPhone 16), `midnight`, `crimson`, `emerald`, `purple`.

**ארכיטקטורה:**
- כל ערכה דורסת משתני CSS תחת `html[data-theme="..."]` ב-`style.css`. נשמר ב-`analyticsPrefs.colorTheme`.
- החלה מוקדמת ע"י סקריפט inline ב-`<head>` (לפני paint) למניעת הבזק; `initColorTheme()` ב-DOMContentLoaded כגיבוי.
- `applyColorTheme(id, el)` / `syncThemePicker()` / `themeVar(name, fallback)` ב-`workout-core.js`.

**לקח קריטי — למה גרסה ראשונה (16.38) הרגישה זניחה:** דריסת `--accent` לבדה לא מספיקה. הזהות הכחולה הייתה
קשיחה בעשרות נקודות: **75 מופעי `rgba(10,132,255,α)`** ב-CSS, ~14 `#0A84FF` ישירים, וגרפי SVG ב-JS
(`drawMicroLineChart`, דונאט, bodylog) עם hex קשיח שלא מגיב ל-CSS.
**הפתרון (16.39):** טוקן `--accent-rgb: R, G, B` ב-`:root` + כל ערכה → ואז `rgba(var(--accent-rgb), α)`
בכל 75 המקומות. הגרפים קוראים `themeVar('--accent')` בזמן ציור (לא hardcode). רקע `body::before` עבר
לשתי שכבות `--ambient-1/2` בעוצמה מורגשת (aurora/eclipse לכל ערכה). `<meta theme-color>` מתעדכן דינמית.

**מה נשאר קבוע בכוונה (סמנטיקה):** `--type-b/c/free`, danger אדום, success ירוק, `HEATMAP_MUSCLE_COLORS`
(מפת שרירים), `HOME_PR_COLORS` (Bench ירוק/OHP כתום), `nutri-log-dot--cut`, ופלטת בורר הצבעים
ב-`editor-logic.js` (בחירת משתמש, לא ערכה).

## שפה עיצובית — Liquid Obsidian → Deep Obsidian (v15.81)

**v15.81 — מערכת surface אחידה.** לפני: 4+ רקעי כרטיסיות מתחרים (`rgba(31,31,31,0.55)` glass / `#1b1b1b` solid / `rgba(31,31,31,0.45)` obsidian / gradient), 3 ערכי radius, צללים מומצאים מקומית, glassmorphism כבד לא-עקבי. אחרי: מערכת טוקנים אחת ב-`:root`.

| טוקן | ערך | שימוש |
|------|-----|--------|
| `--bg` | `#070708` | shell (היה `#0a0a0a`) |
| `--surface-1..4` | `#101013 / #161619 / #1e1e22 / #26262c` | היררכיית עומק — ככל שגבוה יותר, בהיר יותר |
| `--hairline` / `--hairline-hi` | `rgba(255,255,255,.06)` / `.10` | border + border-top ("אור מלמעלה") |
| `--top-glint` | `inset 0 1px 0 rgba(255,255,255,.05)` | תפיסת אור עליונה |
| `--elev-1..3` | צל ambient+key מדורג | elevation אחיד |
| `--r-xs/sm/md/lg/pill` | `8 / 14 / 20 / 28px / 9999px` | סולם radius אחיד (P3) |
| `--dur-1/2/3` + `--ease(-spring)` | `120/200/320ms` | סולם מושן אחיד (P4) |

**Polish Pass P1–P5 הושלם (v16.89–16.97, `docs/POLISH_ROADMAP.md`):** טיפוגרפיה על טוקני
`--fs-*` + tabular-nums; צבע על surfaces + `--success/--danger(-soft)`; radius/gap/padding על
רשת; מושן על `--dur/--ease` + מצב לחיצה `scale(0.98)` אחיד; רשימות hairline (שקילות/תזונה/
ארכיון), גרבר 36×4 אחיד, `emptyStateHtml()` גלובלי (workout-core.js). פרטי סטיות — במסמך ה-roadmap.

**עקרונות:**
- עומק נבנה מ-**surface hierarchy + hairline + צל**, לא מ-blur. כרטיסיות תוכן הן **solid** (אין `backdrop-filter`).
- glassmorphism נשמר **רק** ב-bottom sheets / modals (מרחפים מעל המסך) — `blur(20px)` עדין.
- כפתורי CTA ראשיים (`.action-card`, `.freestyle-finish-btn`) שומרים צל **צבעוני** מכוון; כל שאר הצללים ניטרליים.
- `body::before` — vignette רדיאלי קבוע (מקור אור עדין בראש המסך → שחור מוחלט בתחתית), עומק ברמת המסך.
- טוקנים ישנים (`--card-bg`, `--ios-radius`, `--border`, `--border-hi`) **מופו מחדש** לטוקנים החדשים — שימושים קיימים לא נשברו.

| מה | ערך |
|----|-----|
| כרטיסיות | `var(--surface-2)` solid, `var(--r-lg)` (28px), `--elev-2/3` |
| פונט ראשי | Heebo 900 לכותרות, `rem` units |
| Pill buttons | `#353535` / `--surface-4`, `border-radius: 9999px`, `align-self: flex-start` |
| Freestyle card | `border: 2px dashed rgba(255,255,255,0.2)` |
| Session strip | `height: 50px+safe-area` עם `box-sizing:border-box` (בלעדיו ה-safe-area נספר פעמיים והפאנל קופץ בגובה — התיקון היחיד ששרד את ה-revert), fixed bottom, `z-index: 199`, מוסתר מחוץ ל-workout flow |

**⏪ REVERT (החלטת משתמש): v15.98–v16.3 בוטלו — האפליקציה הוחזרה ל-v15.97.**
הגרסאות שבוטלו כללו: Wave 1+2 (ליטוש UI, חגיגת PR, טבלת סטים חיה, מחשבון פלטות/חימום), LOG SET ב-strip → רישום מהטבלה, טיימר מנוחה ב-strip, PR/פלטות ב-Live, מלאי פלטות מותאם, וניסיונות תיקון "באג התחתית המורמת". הבאג שרד את כל הניסיונות והמשתמש ביקש לעצור. הקוד שמור בהיסטוריה (`853ed82`…`fa7d0a1`) — אפשר לשחזר פיצ'רים נקודתית בעתיד.

**לקחים מהחקירה (לשימור — הבאג עדיין לא פתור):**
- אומת בניתוח פיקסלים: הבר התחתון מרחף בדיוק ~גובה ה-status bar (~60pt) מעל התחתית אחרי רענון, ומתחתיו נחשף רקע ה-body.
- מנגנונים שנבדקו ולא פתרו לבד: ➊ מקלדת iOS גוללת את ה-window בלי להחזיר ל-0 (scrollTo(0,0) על focusout); ➋ `window.location.reload()` ב-standalone+black-translucent מייצר layout viewport קצר (הוחלף ב-`location.replace()` + נדנוד `_kickViewport`).
- `position:fixed` בתוך `.content-area` (scroller) לא אמין ב-iOS לאלמנטים צפים.
- כיוון חקירה עתידי: מדידה חיה ב-Safari remote inspector (`innerHeight` / `visualViewport.height+offsetTop`) ברגע שהבאג פעיל; או מעבר ל-layout שלא מסתמך על fixed — app-container כ-grid עם שורת bars אמיתית ב-flow.

---

## חוב טכני פתוח

| # | תיאור | חומרה |
|---|-------|-------|
| 1 | `details` ב-ArchiveEntry לא שומר cluster per-round — רק per-exercise | נמוכה |
| 2 | archive entries ישנים (לפני 14.12.0-24) חסרים שדה `week` — AI block comparison נופל ל-fallback | נמוכה |
| 3 | ✅ נפתר — `updatePlanFloatBtn` כבר לא מחפש `.header-tools`, רק מנקה כפתורים מוזרקים | — |
| 4 | ✅ נפתר — הארכיון מפוצל ל-chunks (`ARCHIVE_CHUNK_SIZE=20`, `archive_meta`+`archive_N`). הקובץ הגולמי של MFP מפוצל גם הוא (`nutrition_raw_meta`+`nutrition_raw_N`, 1000 שורות/מסמך). מסמך `config` נשאר קל (נתונים קטנים) | — |
| 5 | עריכת סט במסך הסיכום אחרי שנוצר `aiSummary` לא מרעננת אותו — הסיכום עלול להפוך לא-מסונכרן | נמוכה |
| 6 | המלצת AI לסט (set-rec) מציגה תמיד יחידת kg — גם בתרגילי פלטות/משקל גוף | נמוכה |
| 8 | inline styles ב-`index.html` עם radius/spacing קשיחים — מחוץ לתחולת Polish (P3) | נמוכה |
| 9 | transitions מ-JS לא על טוקני מושן: sheet snap-back (`workout-core.js`), טבעת (`food-logic.js`) | נמוכה |
| 7 | ✅ נפתר (v16.81) — עריכת סט details-only כעת בונה מחדש את הסטרינג לפי mode (kg/פלטות/BW), לא קשיח kg | — |
| 10 | ✅ נפתר ברובו (v17.12) — **Audit v17.05** (`docs/BUG_AUDIT_v17.05.md`): תוקנו #1-#18, #20-#23 + נוקה הקוד המת (zxing 332KB, ‎~450 שורות CSS מת, פונקציות ללא קוראים). קריאות Gemini מאוחדות כעת ב-`_geminiRequest` (workout-core) — **אין לשכפל לולאת fetch, להשתמש בה**. נותרו פתוחים: `openExerciseSettings` תוך-אימון (דורש הכרעת מוצר — להחזיר entry point או למחוק), תנאי מת ב-`_fdMealsHTML` (הושאר בכוונה), ותמונות unsplash שהן עדיין דומיין חיצוני אך נתפסות כעת ב-runtime cache של ה-SW | בינונית |
| 11 | `getArchive()` מפרסר את כל ה-JSON בכל קריאה (51 אתרי קריאה) ו-`renderArchiveList` בונה DOM לכל ההיסטוריה גם בחודשים מכווצים — ישלם ריבית ככל שהארכיון גדל. פתרון עתידי: memo-cache שמתבטל ב-save + בניית חודש עצלה | נמוכה |
| 12 | פונקציות יתומות (dead code) אחרי הסרת כפתורי הגיבוי הישנים מהארכיון (v17.71): `exportData`/`triggerImport`/`importData` (`editor-logic.js`) ו-`exportClaudeFile` (`archive-logic.js`) — כבר לא מקושרות לשום UI. להסיר בהזדמנות אחרי ניתוח השפעה (לוודא שאין קוראים דינמיים/onclick שנותרו). המחליף החי: סנכרון ענן + "ייצוא קובץ מאוחד" בהגדרות | נמוכה |
| 13 | חוסר עקביות שמות למסך "בריאות" (health): המזהה הפונקציונלי בפועל הוא `bodylog` (`ui-bodylog`, `_blTab`, `setBodyTab`, `switchMainTab('bodylog')`, `tabbtn-bodylog`) — עקבי ותקין. אבל שריד השם הישן "Composition" נשאר בשם פונקציה אחת `goToComposition` (call sites: `index.html` כרטיסי הבית) ובהערות/תיעוד, בעוד תווית ה-UI היא "health". **המחרוזת "composition" אינה load-bearing** — אין storage key / id ניווט / שדה סנכרון שנשען עליה, ולכן אי-ההתאמה קוסמטית בלבד (חוב קוגניטיבי, לא באג ריצה). **הוחלט מפורשות לא לגעת** (סיכון רגרסיה קטן אך מיותר — רינה־מ של הפונקציה מחייב לעדכן את 2 ה-onclick בו-זמנית). אם יישור שמות בעתיד — משימה מכוונת נפרדת | נמוכה |

---

## שיטות משקל לתרגיל — kg / פלטות / משקל גוף (v15.98)

- **מודל:** `weightMode: 'kg'|'plates'|'bw'` על התרגיל (עורך התרגיל); דגל `isBW` ישן ממופה ל-`bw` כש-`weightMode` חסר. רשומת לוג מקבלת `wm` רק כשהשיטה אינה kg.
- **החלפה תוך כדי אימון:** לחיצה על תווית כרטיס המשקל (`cycleWeightMode`) — נשמרת ב-`state.sessionWeightModes` (חד-פעמית לאימון, לא משנה את הגדרת התרגיל).
- **ווליום:** פלטות = `פלטות × PLATE_KG(=2.5) × חזרות`; משקל גוף = 0 (חזרות בלבד) — החלטת מוצר.
- **פורמט סטרינג סט בארכיון self-describing:** `"80kg x 5"` / `"5 פלטות x 10"` / `"BW x 12"`. כל הפרסרים (`_setStrVol`, `_parseSetString`, `parseSetsFromStrings`) מטפלים בשלושת הפורמטים. **אין לשנות פורמט בלי לעדכן את כולם.**
- **prefill:** `saveWeight` נשמר רק כשהשיטה תואמת את ברירת המחדל של התרגיל — override זמני לא מזהם אימונים הבאים.
- **אחידות פיקרים (v16.01):** ההחלפה זמינה גם ב-Live (`live-edit-sheet`) — תווית כרטיס המשקל לחיצה, `_syncLiveWeightModeUI` מסנכרן תווית/יחידה/+-. **לקח:** ה-Live הוא חזית שנייה לאותם pickers — כל פיצ'ר בפיקרים חייב לחול גם שם (כלל ב-CLAUDE.md).
- **מנוע פיקרים עצמאי למודאל עריכת סט — יומן+ארכיון (v16.81):** `edit-set-modal` (נפתח מהיומן הפעיל ומהארכיון, כולל רשומות details-only ישנות בלי `log`) קיבל ממשק stepper-card זהה ויזואלית לפיקרים הראשיים (משקל⇄/+-/הזנה ידנית, אותו CSS גלובלי בלי שינוי), אבל **לא** מחובר ל-`<select>`-ים הגלובליים/ל-`state.currentExName` — כי הוא עלול לערוך סט מתרגיל לא-פעיל או מאימון ארכיון בלי session בכלל. מנוע נפרד ומקומי: `_editModalMode`/`_editModalVals` + `_editModalInit/_editModalRenderModeUI/_editModalRenderValue/_editModalCycleMode/_editModalStep/_editModalEditValue` (workout-core.js). מצב ההתחלה נקרא מ-`entry.wm||'kg'` (log) או מ-`_parseSetString` המורחב (details-only — מזהה `BW`/`פלטות`/`kg` מהסטרינג ומחזיר `mode`). שמירה: `saveSetEdit`/`saveArchiveSetEdit` כותבים את ה-mode בחזרה ל-`entry.wm` (log) או בונים מחדש את הסטרינג בפורמט הנכון לפי מצב (details-only) — **פותר את חוב #7 למטה** (עריכת סט details-only הייתה תמיד נכתבת מחדש כ-kg).

---

## סיכום מאמן אוטומטי (Coach Summary) — v15.47

- **שמירה מיידית:** `finish()` שומר את האימון לארכיון מיד (upsert לפי `state.archivedTimestamp`), לא רק ב-`copyResult`. מונע אובדן מידע אם המשתמש לא מסיים. `_saveToArchive` הפך ל-upsert.
- **טריגרים:** כל אימון → סיכום אימון; מתג "סיום שבוע" (תפריט שלוש-נקודות, ברירת מחדל ON בשבת, `state.weekEndFlag`) → סיכום שבועי+השוואה; +`week===3` → סיכום בלוק.
- **`_callGeminiOneShot(prompt, {freeText:true})`** — מצב טקסט חופשי (2048 טוקנים) בנוסף למצב JSON המקורי.
- **פרומפטים ניתנים לעריכה:** `StorageManager.COACH_PROMPT_DEFAULTS` + override ב-`KEY_COACH_PROMPTS`, נערכים ב-`coach-prompts-sheet`. מסונכרנים ב-`saveConfigToCloud` (`coachPrompts`).
- **`aiSummary` מסונכרן אוטומטית** דרך `saveArchiveToCloud` (שולח כל הארכיון as-is, ללא whitelist).
- שני מתגי העתקה: מסך סיכום (`KEY_COPY_INCLUDE_COACH`) וארכיון (`KEY_ARCHIVE_COPY_COACH`, ברירת מחדל כבוי). **שניהם נפרדים** — מקור אמת כפול (חוב ידוע; משתמש עלול לכבות באחד ולקבל coach מהשני).
- **קובץ לקלוד (v15.84+):** `exportClaudeFile()` (כל הלוג) ו-`executeDownloadByRange()` (טווח, מתוך `range-copy-sheet`) מורידים **JSON** של לוג האימונים (`{app,scope,workouts_count,includes_coach_summary,generated,workouts[]}`, כרונולוגי עולה). מכבד את מתג סיכום המאמן של הארכיון (`KEY_ARCHIVE_COPY_COACH`) — כשכבוי, `aiSummary` מוסר מכל רשומה. נפרד מ-`exportData()` שהוא גיבוי JSON מלא (כל ה-localStorage, לשחזור).

### זיכרון מאמן — קונטקסט ארוך-טווח (v15.86)
המאמן זוכר תובנות מעבר לחלון ה-10 הודעות, **בלי לפגוע במהירות התגובה** (כל התוספת היא צד-קלט / רקע).
- **ביצועי AI (v16.03):** `_callGeminiOneShot` זוכר את המודל האחרון שהצליח (`gympro_ai_pref_model`) ובודק אותו ראשון — מפתח שחוטף 404/429 במודל הראשון לא משלם סיבוב מבוזבז בכל קריאה. נוסף timeout לניסיון (20ש'/60ש' freeText). המלצת plateau באנליטיקה רצה כ-freeText — מצב ה-JSON הכפוי התנגש עם פרומפט "plain text" וגרם לאיטיות.
- **#1 ניתוחים קודמים:** `_buildCondensedCoachSummaries(2, 800)` מזריק ל-`buildSystemPrompt` את 2 סיכומי המאמן האחרונים (`aiSummary`), מקוצרים ל-~800 תווים. צד-קלט בלבד → השפעה זניחה על latency.
- **#2 זיכרון מתגלגל:** `KEY_COACH_MEMORY` (`{text, coveredLen, updatedAt}`). `_coachMemorySection()` מזריק אותו ל-`buildSystemPrompt`. הרענון (`_updateCoachMemory`) רץ **ברקע, off-critical-path** — מופעל ע"י `_maybeUpdateCoachMemory()` אחרי הצגת התשובה, רק כשנצברו ≥`COACH_MEMORY_THRESHOLD` (20) הודעות חדשות. משתמש ב-`_callGeminiOneShot(freeText, maxTokens:700)`. **לעולם לא לתמצת סינכרונית לפני תשובה** — זו קריאת API נוספת שתכפיל latency.
- **סנכרון ענן (v15.87):** הזיכרון נשמר ב-doc `ai_history` בפיירבייס יחד עם הצ'אט (`coachMemory` field) — גיבוי אוטומטי ב-`closeAICoach`, שחזור דרך כפתור "שחזר היסטוריה". מסונכרן בין מכשירים בדיוק כמו השיחות.
- חלון הצ'אט החי נשאר 10 הודעות (`callGeminiAPI`). `clearAIHistory` מאפס גם את הזיכרון. `coveredLen` מקבל clamp ב-`openAICoach` (היסטוריה מוגבלת ל-300).

---

## גשר אפל-ווטש — אימון חי שעון⇄טלפון (v15.92, Two-lane union)
מטרה: לתעד אימון מ-Apple Watch בלי native/App Store/$99, עם **טרנזיטיביות מלאה דו-כיוונית**. שעון = **Apple Shortcuts** → Apps Script proxy (`docs/watch-bridge.gs`) → Firestore `gympro_data/live_session` → ה-PWA קורא ב-`onSnapshot` ומסכם.
- **ייצוג ה-doc (v15.92):** `{ active:bool, data:"<json>", wlog:"<json>" }` — **שני מסלולים נפרדים**: `data`=מסלול הטלפון (metadata + סטי-`'p_'`, נכתב רק ע"י ה-PWA), `wlog`=מסלול השעון (סטי-`'w_'` + מצביע-תרגיל, נכתב רק ע"י ה-proxy, append-only). `_unwrapLive` ממזג את שניהם בקריאה (union לפי setId; `currentExName` מהמסלול עם `currentTs` חדש; `setIdx` **נגזר** מהאיחוד).
- **clobber נפתר מבנית (v15.92):** אף צד לא כותב לשדה של השני (`set(merge)`/`updateMask` = מיזוג ברמת-שדה) → אין lost-update, ללא transactions. קודם (v15.90 forceAdopt, v15.91 read-merge-write) נגעו בסימפטום והשאירו חלון מרוץ.
- **`WatchBridge`** (workout-core.js) — **כבוי כברירת מחדל** (`KEY_WATCH_BRIDGE_ON`), no-op מוחלט כשכבוי. `onStateSaved()` (hook יחיד ב-`saveSessionState`, debounce 400ms, hash) → `_doPublish()` כותב מסלול-טלפון בלבד; בתחילת סשן `resetWlog=true` מנקה מסלול-שעון מסשן קודם. `_adopt()` עם gating על `_wlogRev` (מתעלם מ-echo עצמי); `forceAdopt()` על load/restore/visibilitychange/focus.
- **מיטיגציות סיכון (מהחקירה):** `setId` לכל סט (dedupe R3) · timestamp ארכיון = `liveSessionId` (R5, `finish`) · `clearLiveSession` ב-`copyResult`/`discardSession` (anti-zombie R4) · ה-proxy מוודא `active`+`sessionId`, מנרמל RIR למחרוזת (R1), מוודא w/r (R2) · טיימרים **לא** מסונכרנים דרך ה-doc (R8) · `enablePersistence` (R12) · timeouts לכל קריאה (R13).
- **scope:** השעון append-only (`logSet/nextExercise/getState`); clusters/1RM/swap-order/interruption מנוהלים בטלפון.
- **⚠️ חוב/בדיקה:** הלוגיקה של ה-live-sync (merge/adopt/ping-pong) **לא נבדקה על מכשירים אמיתיים** — דורשת אימות end-to-end עם Firebase+שעון. ה-proxy וה-Shortcut מותקנים בידי המשתמש (service-account + deploy + הרכבת הקיצורים). מגבלה: השעון צריך רשת בחדר הכושר.

---

## גשר תזונה Apple Health (v15.99)

- **זרימה:** MFP → Apple Health → קיצור דרך (אוטומציות 10:00/14:00/18:00/23:45) → `health-nutrition-bridge.gs` (PropertiesService, ~120 ימים, בלי Firestore/SA) → ה-PWA מושך JSONP.
- **משיכה:** בכל כניסה לאפליקציה (load + visibilitychange) + כל שעה עגולה כשהיא פתוחה + כניסה לטאב Composition. throttle 15 דק' (`syncHealthNutrition`), שקט לחלוטין במצב אוטומטי.
- **כלל הזהב — MFP מקור האמת:** ימי Health מסומנים `src:'health'` ב-`nutritionDaily`. `mergeHealthNutritionDays` לעולם לא דורס יום MFP; `saveNutritionDaily` (ייבוא MFP) דורס הכל כולל ימי Health. ל-Health אין per-meal — `nutritionRaw` נשאר בלעדי ל-MFP ולא נגעו בו.
- **לימי Health אין `meals`/per-meal** — בייצוא המפורט יום כזה יוצא `source:'summary'` (totals בלבד) עד שייבוא MFP/תיעוד ישיר מוסיף פירוט.
- **מסך תזונה (v16.00):** כרטיס "תזונה היום" (`bl-daily-card`) מעל הממוצע — נתוני היום + חותמת משיכה אחרונה (`KEY_HEALTH_LAST_SYNC`). כל פעולות ייבוא/ייצוא אוחדו ל-bottom sheet אחד (`nutri-io-sheet`) שנפתח מכפתור בכרטיס היומי. **ממוצע התזונה ומנוע ה-TDEE מחריגים את היום הנוכחי** (תיעוד חלקי תוך-יומי מ-Health מטה אותם). כרטיס המאזן מקופל כברירת מחדל (hero+קצב נוכחי) — `_blTdeeExpanded`.

## ייצוא/ייבוא — סדר (v16.58)

- **מתג הפעלה/כיבוי לכל גשר:** MFP ו-Health כעת ניתנים לכיבוי (`KEY_MFP_BRIDGE_ON`/`KEY_HEALTH_BRIDGE_ON`, **ברירת מחדל דלוק** — רק `'0'` מכבה, להבדיל מהשעון שכבוי). Gating בנקודה אחת: `syncHealthNutrition` ו-`importNutritionFromGmail` בודקים `is…BridgeOn()` בראש. שלושת ה-`*_ON` בקובץ החיבורים.
- **ייצוא תזונה — בדיוק 2 קבצי JSON**: `exportNutritionDailyJson` (מקוצר: date+cal+macros) ו-`exportNutritionDetailedJson` (מפורט). מ-v17.36 שניהם מקבלים bounds ‏{from,to,slug,label} מבורר הייצוא המשותף (`range-export-sheet`, ‏`openRangeExportSheet`) — לא תלויים בשום צ'יפים על מסך. גם `exportBodyCsv` עבר לאותו בורר. הוסרו: `exportNutritionCsv`, `exportNutritionRawCsv`, `exportFoodDiaryJson`, `bl-export-modal` הישן, `_nutritionRangeBounds`.
- **`_buildNutritionDetailed(from,to)`** (בונה משותף לייצוא הנפרד ולמאוחד) — **קדימות ליום: תיעוד ישיר (`getFoodLogDay`) גובר על MFP**; אחרת MFP; אחרת סיכום. מקור יחיד ליום, בלי ספירה כפולה. כולל `components` של Meal Builder.
- **ווידג'ט אייפון (v17.19):** `buildWidgetSnapshot` (storage.js) — snapshot קומפקטי: תזונה היום + יעדים + `state` תזונתי, משקל + מגמה שבועית (`weekDelta` מול שקילה ≥7 ימים אחורה) + 7 נקודות ספארקליין, אימון אחרון (`type`/sets/volume מ-details). דחיפה: `maybePushWidgetSnapshot` בפתיחה (throttle 10 דק') + `pushWidgetSnapshotBeacon` ביציאה (visibilitychange→hidden, sendBeacon). גשר `docs/widget-bridge.gs` (PropertiesService, snapshot יחיד) + ווידג'ט `docs/widget-scriptable.js` (Scriptable, פריסת "גרסה 2" מ-`docs/mockup-widget.html`, תצוגה בלבד). מפתחות הגשר ב-`_connectionKeys()`. **מגבלת iOS (נבדק בפועל):** אי אפשר לפתוח PWA מבחוץ בכלל — web clip לא מופיע ב-"Open App" של Shortcuts, וקישור לכתובת נפתח בספארי (אחסון נפרד!). לכן אין יעד לחיצה (`TAP_URL` ריק בסקריפט).
- **גיבוי מלא + גיבוי שבועי לאימייל (v17.17):** `buildFullBackup`/`exportFullBackup`/`restoreFullBackup` (storage.js) — צילום גולמי של **כל** מפתחות `gympro_*` (מחרוזות, בלי parse) ⇒ שחזור מדויק ביט-לביט כולל סודות; עמיד לעתיד (מפתח חדש נכנס אוטומטית, בניגוד ל-allowlist של `exportConfiguration`). שחזור = מחיקת כל `gympro_*` ← כתיבה ← reload. גשר חדש `docs/backup-bridge.gs` (doPost → GmailApp לבעל הסקריפט, בלי אחסון); `maybeSendWeeklyBackup` נקרא ב-DOMContentLoaded (delay 4s) ושולח אם עברו ≥7 ימים (`KEY_BACKUP_LAST`), fetch POST עם `text/plain` (בקשה פשוטה, בלי preflight). מפתחות הגשר (URL/TOKEN/ON, כבוי כברירת מחדל) נכללו ב-`_connectionKeys()`. UI: קבוצת "גיבוי שבועי לאימייל" בהגדרות + כפתורי גיבוי/שחזור מלא בקבוצת גיבוי מקומי.
- **פורמט meals אחיד בייצוא (v17.16):** כל יום ב-`nutrition_detailed` מחזיר שדה `meals` באותו מבנה — ימי MFP מומרים דרך `_detailMealsFromMfpRows` (item לכל שורת ארוחה, שם גנרי, מאקרו + `micros`), ימי summary מקבלים `meals: []`. השדה `mfp_rows` הוסר. **סיבה:** LLM שקרא קובץ שבו הימים הראשונים היו `mfp_rows` הסיק ש"אין פירוט" בימי `app` (שדה `meals` שונה). בנוסף `readme` מוטמע בראש שני הייצואים (`_NUTRI_EXPORT_README`) שמסביר את המבנה לקוראים אוטומטיים.
- **קובץ מאוחד (`exportUnifiedData`):** `nutrition_raw_mfp` הוחלף ב-`nutrition_detailed`. כעת = weights + nutrition_daily + nutrition_detailed + workouts.
- **המאגר המקומי בענן בלבד:** `KEY_FOOD_DB`+`KEY_FOOD_LOG` מסונכרנים דרך מסמך `config` (saveConfigToCloud) — אין ייצוא JSON מקומי.
- **קובץ החיבורים:** נוסף `KEY_USDA_KEY`. כלל ב-CLAUDE.md: בכל סוד/אינטגרציה חדשים — לשאול אם לכלול ב-`_connectionKeys()`.
- **סריקת ברקוד חופשית (v16.67):** `fdLiveScanStart` (food-logic.js) משתמש ב-`_FD_CAM_CONSTRAINTS` (1080p, environment) במקום מצלמה בברירת מחדל; `_fdTuneCamera` מפעיל `focusMode:'continuous'` ומזהה תמיכת `torch` (כפתור `fd-live-torch` + `fdLiveToggleTorch`). נתיב iOS (ZXing) עם hints `TRY_HARDER`+`POSSIBLE_FORMATS` ו-`decodeFromConstraints` ברזולוציה גבוהה (fallback ל-`decodeFromVideoDevice`); כיוון מצלמה ב-ZXing דרך `_fdTuneWhenReady` (polling ל-`video.srcObject`, לא נסמך על promise הסורק). לולאת native עברה מ-RAF ל-throttle 120ms. כל ה-capability tuning guarded → no-op במכשירים ישנים. הזיהוי תמיד full-frame; המסגרת רוככה ל-UX סלחני.
- **אבחנת ארוחות בצבע (v16.66):** `_FD_MEAL_ACCENT` (food-logic.js) ממפה ארוחות ברירת מחדל לגוון עדין (בוקר=ענבר/צהריים=ירוק/ערב=אינדיגו/נשנוש=סגול). `_fdMealsHTML` מזריק `--fd-meal-accent` ל-`.fd-meal`; ה-CSS (`.fd-meal` border-inline-start, `.fd-meal-icon`, `.fd-meal-add`) צובע דרך `color-mix(... var(--fd-meal-accent, var(--accent)) ...)`. ארוחה מותאמת/ששמה שונה → נופל ל-`var(--accent)`. מוקאפ ההשוואה: `docs/mockup-meal-distinction.html`.
- **נראות יומן מזון לסוכן (v16.65):** `_buildFoodDiaryAIContext(slim)` (workout-core.js) מזריק ל-system prompt את פירוט `getFoodLog()` — היום לפי ארוחות+פריטים+מאקרו, יעדי `getAnalyticsPrefs`, ו-7 ימי היסטוריה (guard ~3K, `!slim` בלבד), אחרי `_buildNutritionAIContext`. תוקן תיוג `src==='app'`→"יומן פנימי" (היה "MyFitnessPal"). הסוכן ראה קודם רק סיכומי NUTRITION_DAILY, לא רזולוציית מזון/ארוחה.
- **מרכיב מהמאגר (v16.64):** ב-Meal Builder כפתור "חפש מרכיב" (`fdMealSearchComponent`) פותח את שיט החיפוש במצב `_fdCompPickMode` (body class `fd-comp-pick` מרים z-index מעל ה-Meal Builder). בחירת תוצאה → `_fdAddComponentFromFood` (במקום `_fdOpenPortion`) מוסיפה כמרכיב עם per100. `closeFoodAdd`/`_fdOpenPortion` מאפסים את המצב. כפתור "ידני" נשאר להזנה ידנית.
- **זרימת ברקוד (v16.62):** `resolveBarcode(code)` — resolver מרוכז: `KEY_FOOD_DB` לפי ברקוד (מיידי, offline, נסרק בעבר) → OFF (`lookupBarcode`). מחליף קריאות ישירות ב-`fdOnPhoto`/`_fdLiveOnHit`. בהחטאה: `_fdPendingBarcode` מצמיד את הברקוד למזון שייבנה מ-OCR תווית (`_fdLabelViaGemini`, id `off:<barcode>`) → זיהוי חד-פעמי שנשמר ל-cache ומסונכרן. בסריקה חיה — כפתור "צלם תווית" (`fdLiveSnapLabel`, רק עם מפתח Gemini). `_isIsraeliBarcode` (729) להתאמת ההודעה. קטלוג שקיפות-מחירים נדחה (אין API חי).
- **צמ"ת (v16.61):** מקור גנרי עברי רשמי דרך CKAN `data.gov.il` (`searchTzameret`, resource `c3cb0630...`, per-100g, שדות `shmmitzrach`/`food_energy`/`protein`/`total_fat`/`carbohydrates`). מקביל ל-OFF/USDA ב-`searchFoods`, **בראש התוצאות**, cache ל-`KEY_FOOD_DB` → offline+סנכרון. כשל/CORS → `[]` ללא רגרסיה. צ'יפ `tzameret`. (תלוי ב-CORS של data.gov.il — לאמת בדפדפן.)
- **grams מצרפי (v16.59):** בפריט מנה עם `components`, ה-`grams` ברמת-הפריט הוא 0 (המשקל ברכיבים). `_detailMealsFromEntries` דורס `item.grams = sum(components.grams)`. עיקרון: כל שדה מצרפי ברמת-פריט = סכום עקבי של הרכיבים (יש console.warn אם kcal חורג).

---

## מסך Composition + אינטגרציית MyFitnessPal (v15.59–15.76)

מסך "Composition" (`ui-bodylog`, `bodylog-logic.js`) מחולק ל-2 טאבים בבורר `segment-wrapper`/`seg-btn` (זהה לאנליטיקה/ארכיון):
- **שקילה** — משקל + אחוז שומן, גרפים, היסטוריה, צילום/OCR, ייבוא/ייצוא CSV.
- **תזונה** — נתוני MyFitnessPal: כרטיס ממוצעים דינמי (קלוריות/חלבון/פחמימה/שומן) לפי טווח 7/30/90/הכל/**מותאם**, גרף קלוריות + גרף חלבון, היסטוריה יומית, 4 ייצואים — סיכום יומי (הכל/תקופה) + גולמי MFP per-meal (הכל/תקופה), וכפתור איפוס. ייצוא "תקופה" (יומי וגולמי כאחד) מכבד את בורר הטווח בראש המסך דרך `_blFilter`; הגולמי מסנן שורות לפי `_parseFlexDate(row[dateIdx])` (v15.83).

**הגשר (Apps Script, `docs/mfp-nutrition-bridge.gs`):** רץ בחשבון ה-Gmail של המשתמש; מאתר את ייצוא ה-MFP האחרון, מוריד את ה-ZIP מ-S3 (עוקף CORS בצד-שרת), מאגד תזונה לפי יום ומחזיר JSON + `rawCsv`. מוגן ב-`SECRET_TOKEN`.
- **JSONP חובה:** `fetch` חוצה-מקור ל-Apps Script נכשל תמיד (ה-redirect ל-googleusercontent חסר כותרות CORS). הקריאה דרך `<script>` (JSONP, פרמטר `callback`) — `_jsonpRequest` ב-workout-core.
- **מיזוג ללא כפילויות:** `saveNutritionDaily`/`saveNutritionRaw` עושים upsert לפי תאריך — קובץ חדש דורס ימים חופפים. משיכה שבועית מצטברת.
- **ניקוי ספייקים בגרף בלבד:** ימים <50% מהממוצע (הזנה חסרה) מוחלפים בממוצע (`_cleanNutriOutliers`). הממוצעים/הרשימה/הייצוא עם הנתונים האמיתיים.
- **tooltip:** יחידה רק היכן שרלוונטי — ק"ג למשקל, % לשומן, ללא יחידה בתזונה.

## מנוע TDEE / מאזן אנרגיה (v15.77–15.79)

`computeTDEE()` ב-`bodylog-logic.js` (offline). כרטיס בטאב תזונה; עוגן ל-AI ב-`_buildTdeeAIContext()`. מתודולוגיה מלאה: `docs/tdee-methodology.md`.
- **רב-שיטתי:** מדידה (back-calc) + Katch-McArdle + Cunningham (ממשקל+%שומן) + Mifflin (מפרופיל גוף). מציג טבלת השוואה + טווח ביטחון, לא מספר בודד.
- **מדידה = `avgIntake − slope×7700`** (slope ק"ג/יום מרגרסיה על המשקל). מעגן כשיש מספיק נתונים.
- **דיוק — לקחים קריטיים (v15.78):** חלון רגיל מערבב שלבים (תחזוקה+קאט) וקפיצות כיול-משקל → TDEE מנופח. לכן: (1) פילוח ל**שלב התזונתי הנוכחי** (מלוג המעברים), (2) **דילוג על 7 ימים ראשונים** של השלב (מים), (3) הנמכת ביטחון+הרחבת טווח (±12%) כש-RMSE>0.7 או <14 ימים, (4) חסם שפיות: קצב >1.6 ק"ג/שבוע = נתון פגום → לא מעגנים.
- **מכפיל פעילות:** מ-`getBodyProfile().activity` (ברירת מחדל 1.55). משפיע **רק על שורות התחזית**, לא על המדידה. ניתן לשינוי בהגדרות → פרופיל גוף.
- **פרופיל גוף** (`KEY_BODY_PROFILE`): מין/גיל/גובה/פעילות — אופציונלי, מסונכרן בקונפיג הענן.

## גישת AI לנתונים (v15.74)

`buildSystemPrompt` מזריק לכל קריאת Gemini גם **תזונה בפועל** (ממוצעי 7/30 + פירוט 14 ימים) ו**הרכב גוף/שקילות** (משקל אחרון, מגמת 30 יום, 12 שקילות). במצב `slim` (תוך-אימון) רק ממוצעים. עדכון שורת ה"מקורות" כדי שה-AI ידע שהנתונים זמינים.

## ניווט בהחלקה בין טאבים (v15.68–15.70)

`_initTabSwipeGesture` (workout-core) — החלקה אופקית על 4 מסכי הטאבים מחליפה טאב (RTL-aware, "התוכן עוקב אחרי האצבע"). מוחרגים: גלילה אנכית, מודאלים/sheets, גרפים (svg/canvas), ואלמנטים שגלילים אופקית (`_hasHorizontalScroll`). ה-`touchmove` **לא-passive** עם `preventDefault` על החלקה אופקית נעולה — חוסם את גסט ניווט-הקצה של ספארי (המסך הלבן). אין התנגשות עם swipe-back (מסכי הטאבים ב-`NO_BACK_SCREENS`).

## סנכרון ענן — מבנה Firestore (v15.75)

קולקציה אחת `gympro_data` עם מסמכים נפרדים, כל אחד מתחת ל-1MB:
- `archive_meta` + `archive_0/1/...` — ארכיון אימונים (20/מסמך).
- `nutrition_raw_meta` + `nutrition_raw_0/1/...` — קובץ MFP גולמי per-meal (1000 שורות/מסמך). ה-`header`+`dateIdx` ב-meta.
- `config` — תוכניות, תרגילים, prefs, מצב תזונתי, **תזונה יומית** (`nutritionDaily`), **שקילות** (`bodylog`), **פרופיל גוף** (`bodyProfile`), coachPrompts. נתונים קטנים → מסמך בודד בטוח.
- `ai_history` — היסטוריית שיחות.

**מניעת 1MB מוחלטת:** כל דאטה שגדל ללא חסם (ארכיון, raw תזונה) מפוצל ל-chunks עם ניקוי עודפים אוטומטי. סנכרון אוטומטי: ארכיון אחרי אימון; config+raw אחרי ייבוא תזונה (fire-and-forget, מוגן ב-`_ensureReady`).

---

## לקחים מצטברים

- **משקלי פונט חייבים להיטען בקישור Google Fonts** (v16.89, P1) — עד אז ה-CSS השתמש ב-`font-weight:700`
  ב-131 מקומות אבל הקישור טען רק 300/400/600/800/900 — הדפדפן עיגל 700→800 בשקט. כל הוספת משקל ב-CSS
  מחייבת עדכון ה-`<link>` ב-index.html. סולם P1: 900/700/600/400 (+200/300 thin לספרות hero בלבד).
  טוקני `--fs-*` ב-:root — כל font-size חדש חייב טוקן, לא ערך חופשי (docs/POLISH_ROADMAP.md).

- **תאריכים: תמיד מקומי, לעולם לא `toISOString().slice(0,10)`** (v16.04) — UTC החזיר את "אתמול" בין חצות ל-03:00 שעון ישראל; היום שהסתיים לא "נסגר" בתזונה (נשאר מסונן מהיסטוריה/ממוצע/גרפים) עד לפנות בוקר. תוקן ב-`_blTodayStr`/`_blCutoff`/`StorageManager._todayStr`. שמות קבצים להורדה נשארו UTC — קוסמטי.


- **שדרוג UI/UX מול מתחרים (v15.98–15.99):** Wave 1 — קונטרסט AA (`--text-dim`→`#A6A6AD`), יעדי מגע ≥44px (::after hit-area), `100dvh`, `emptyStateHtml()` גלובלי, חגיגת PR (`_getHistoricalMaxE1RM`+`_celebratePR` ב-nextStep, סימון `isPR` בלוג). Wave 2 — `renderSetSessionTable()` (טבלה חיה + ghost מ-`getLastPerformances`+`parseSetsFromStrings`, מתרענן מ-initPickers/nextStep/saveSetEdit), מחשבון פלטות (`_calcPlates`, פרף `KEY_BAR_WEIGHT`), פירמידת חימום (`WARMUP_SCHEME`, pill בסט 1 כש-w≥40), toggle `KEY_SKIP_CONFIRM` שמדלג על ui-confirm דרך `confirmExercise(true)` (לא במצב סבב). Wave 3 (מוטיבציה) ו-Wave 4 ממתינים — ראה ROADMAP "כיוונים פתוחים".
- **Audit באגים עמוק (v15.97):** נמצאו ותוקנו — `saveData` לא דיווח כשל quota (עכשיו מחזיר bool, `_saveToArchive` מתריע); "שחזר מהענן" דרס ארכיון מקומי בלי אישור (נוסף `showConfirm`); `initPickers` קרס על `setIdx` מעבר לגבול בחזרה לתרגיל שהושלם (נוסף clamp); שמות עם גרשיים/`<` שברו `onclick` inline ו-innerHTML (נוספו `escapeHtml`/`escapeJsAttr` גלובליים ב-workout-core.js — להשתמש בהם בכל הזרקת שם!); `_parseFlexDate` ייבא תאריכים לא-קלנדריים ("12/13"); ל-SW fetch לא היה catch לכשל רשת.
- `git merge --theirs` יכול לכסות commits מ-main. תמיד לפתור conflicts ידנית.
- `localStorage.setItem` ללא try-catch = אובדן נתונים שקט ב-QuotaExceeded. כל שמירה = עטופה.
- לעולם לא `JSON.stringify(userObj)` בתוך `onclick=""` — HTML injection. העבר רק מזהה (timestamp).
- session-timer-strip נשלט אך ורק דרך `navigate()` (ו-`restoreSession()` שמשכפלת אותו). אין מקום אחר לגעת בו.
