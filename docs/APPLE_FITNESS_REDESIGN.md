# עיצוב מחדש — שפת Apple Fitness

מסמך זיכרון פרויקט. מתאר את עבודת ה-redesign של GYMPRO ELITE לשפה הוויזואלית של
אפליקציית **Apple Fitness** (מסך Summary). מטרתו לשמש **בסיס להמשך** — כל סשן חדש
שממשיך את המהלך צריך לקרוא אותו קודם, לא להמציא מחדש את הטוקנים.

> **טריגרים שמחייבים קריאה של מסמך זה:** "המשך redesign", "Apple Fitness", "המסך הבא
> בעיצוב אפל", "ליים / glass / טבעות", כל בקשה על מסך נוסף בשפה הזו, או המשך מהמוקאפ.

---

## 1. מטרה

להעביר את האפליקציה מ-**Liquid Obsidian** (העיצוב הנוכחי) לשפה ויזואלית בהשראת
**Apple Fitness** — ברמת הטוקנים, לא רק צבע: פלטה, טיפוגרפיה, גלאס, אלמנטים, מבנה רשת,
היררכיה. יעד: תחושת "מוצר של Apple", תוך שמירה על כל הפונקציונליות והדאטה הקיימות.

**המהלך הדרגתי — מסך אחר מסך.** קודם מוקאפ להשוואה ואישור כיוון, רק אחר כך יישום ב-CSS.

---

## 2. סטטוס נוכחי

| פריט | מצב |
|------|-----|
| מסך **סיכום אימון** (`ui-summary`) | ✅ מוקאפ v2 מאושר-לכיוון |
| מוקאפ | `docs/mockup-apple-fitness.html` (השוואה side-by-side + מפרט טוקנים) |
| יישום ב-`style.css` | ⏳ טרם — ממתין להחלטת התחלת יישום |
| מסכים נוספים | ⏳ לא התחילו (ראה Roadmap, סעיף 7) |

**המוקאפ מכיל:** את מסך סיכום האימון בשני הצדדים (Liquid Obsidian נוכחי מול Apple Fitness),
אותם נתונים בדיוק (דחיפה A · 5.3 טון · 13 סטים), + מפרט טוקנים מלא + עקרונות.

**היסטוריה:**
- **v1** — מבנה נכון אבל צבעים לא מדויקים (ירוק #30D158 רך, ערכים לבנים, כרטיסים שטוחים).
- **v2** — כויל למסך ה-Summary האמיתי: ליים-נאון, ערכים צבועים, גלאס, אלמנטים מקוריים.

---

## 3. עקרונות השפה (Apple Fitness)

1. **ערך לפני קישוט** — מספר גדול ב-SF Rounded מקבל מקום; ההיררכיה מגודל וצבע, לא ממסגרות/גרדיאנטים.
2. **הערך צבוע, לא לבן** — כל מדד לובש את צבע הסמנטיקה שלו (נפח=ליים, מאמץ=ורוד, מוכנות=ציאן). הצבע *הוא* המידע.
3. **המשטח מפריד, לא הקו** — אין גבול 1px לכרטיס. ההפרדה מ-`#000` מול כרטיס גלאס, ו-hairline פנימי בין שורות בלבד.
4. **גלאס אמיתי** — כרטיסים עם גרדיאנט עדין + top-glint; ניווט = טאב-בר מרחף עם `backdrop-filter: blur`.
5. **הפונט של המכשיר** — מחסנית מערכת `-apple-system` → SF Pro / SF Hebrew נייטיב באייפון. **לא מטמיעים** SF (קנייני).
6. **מבנה רשת** — hero (טבעות/מדד ראשי) → זוגות כרטיסים ריבועיים עם גרפים ו-chevron → פירוט.
7. **צבע = סמנטיקה, לא מותג** — הכחול מפסיק להיות "צבע המותג" והופך לעוד גוון אחד (מרחק/קישורים).

---

## 4. הטוקנים המאושרים

### צבעים (נדגמו ממסך Summary אמיתי)
| טוקן | ערך | שימוש |
|------|-----|-------|
| `--lime` | `#B5FF2B` | ראשי — capsule, טבעת Exercise, טאב פעיל, נפח |
| `--lime-deep` | `#7BE000` | קצה גרדיאנט / hover |
| `--pink` (Move) | `#FF2D55` | מאמץ / עצימות |
| `--cyan` (Stand) | `#22E0E8` | מוכנות / התאוששות |
| `--purple` | `#CF8BFF` | ערכי מונה, גרפים |
| `--blue` | `#0A84FF` | מרחק / קישורים בלבד |
| `--orange` | `#FF9F0A` | RIR קשה / אזהרה |
| `--yellow` | `#FFD60A` | זמן |

### גלאס ומשטחים (Dark)
| טוקן | ערך |
|------|-----|
| רקע shell | `#000000` (שחור טהור) |
| כרטיס גלאס | `linear-gradient(180deg,#1F1F22,#161618)` |
| top-glint | `inset 0 .5px 0 rgba(255,255,255,.07)` |
| טאב-בר גלאס | `rgba(44,44,48,.55)` + `backdrop-filter: blur(26px) saturate(180%)` |
| chevron / צ׳יפ | `rgba(118,118,128,.26)` |
| מפריד פנימי | `rgba(84,84,88,.42)` (hairline inset) |
| label ראשי | `#FFFFFF` |
| secondaryLabel | `rgba(235,235,245,.6)` |
| tertiaryLabel | `rgba(235,235,245,.32)` |

### טיפוגרפיה
- משפחה: `-apple-system, "SF Pro Display", "SF Pro Text", system-ui` (עברית: SF Hebrew נייטיב).
- מספרים/ערכים: `ui-rounded, "SF Pro Rounded"` + `font-variant-numeric: tabular-nums`.
- סולם: כותרת מסך 34/Bold · כותרת כרטיס 22/Bold · headline 17/Semibold · body 15 · יחידה קטן uppercase (אותו צבע כמו הערך).
- **כלל זהב:** משקל כותרת 700 (לא 900 של Heebo).

### צורה
| טוקן | ערך |
|------|-----|
| רדיוס כרטיס | `22px` continuous |
| רדיוס כפתור | capsule (`980px`) |
| שוליי מסך | `18–20px` |
| כפתור ראשי | ליים מלא, טקסט שחור (לא gradient כחול) |

---

## 5. מיפוי Liquid Obsidian → Apple Fitness

| ממד | נוכחי | Apple Fitness |
|------|--------|---------------|
| רקע | `#070708` | `#000` טהור |
| כרטיס | `#161619` + גבול 1px | גלאס גרדיאנט, ללא גבול |
| ירוק | `#30D158` רך | `#B5FF2B` ליים-נאון |
| פונט | Heebo 900 | SF Pro / SF Hebrew |
| מספרים | Heebo bold | SF Rounded tabular |
| ערכים | לבן | **צבועים** לפי מדד |
| כפתור | gradient כחול r=16 | capsule ליים מלא |
| מבנה | עמודה אחת | רשת 2 טורים + hero |
| ניווט | tab-bar אטום | גלאס מרחף |

---

## 6. הערות טכניות ליישום

- **RTL:** `flex-start` = ימין. chevron "קדימה" = `‹` (שמאלה). טבעות/אייקון בצד ה-leading (ימין).
- **פונט מערכת:** אין להטמיע SF. מחסנית `-apple-system` מספיקה — נייטיב באייפון, fallback בדפדפן.
- **אייקונים:** במוקאפ יש אייקוני טאב-בר (SVG) — זה תיאור של UI של Apple. בייצור, לפי כלל הפרויקט
  ("אין אייקונים על כפתורים"), להחליט בנפרד אם טאב-בר מקבל אייקונים (חריג נחוץ ל-Apple-like) או טקסט בלבד.
- **קבצים שיושפעו ביישום:** `style.css` (טוקנים + מחלקות מסך), `index.html` (מבנה מסך), `workout-core.js`
  (`buildSummaryUI` — מרנדר את ה-HTML של הסיכום). לכל מסך — לזהות את פונקציית הרינדור שלו קודם.
- **תאימות ערכות צבעים:** לאפליקציה יש כבר מערכת themes (`data-theme`). "Apple Fitness" יכול להיות
  ערכת theme נוספת או החלפת ברירת המחדל — **החלטה פתוחה** (סעיף 8).
- **כלל גרסה:** יישום ב-`style.css`/`index.html`/`workout-core.js` = bump `sw.js` + `version.json` (כרגיל).
  מסמכי docs ומוקאפים — ללא bump.

---

## 7. Roadmap מסכים

| # | מסך | פונקציית רינדור עיקרית | סטטוס |
|---|------|------------------------|--------|
| 1 | סיכום אימון (`ui-summary`) | `buildSummaryUI` (`workout-core.js`) | ✅ מוקאפ v2 · `docs/mockup-apple-fitness.html` |
| 2 | מסך בית / dashboard (`ui-week`) + tab-bar גלובלי | `navigate`, `switchMainTab` | ✅ מוקאפ · `docs/mockup-apple-fitness-home.html` |
| 3 | **מסך אימון פעיל (`ui-main`)** | `workout-core.js` (pickers, session strip) | ⏳ **הבא בתור** |
| 4 | ארכיון (`switchMainTab` archive) | `archive-logic.js` | ⏳ |
| 5 | Composition (משקל/תזונה) | `bodylog-logic.js` | ⏳ |
| 6 | עורך תוכנית / תפריט אימון | `renderWorkoutMenu` (`editor-logic.js`) | ⏳ |

**תהליך לכל מסך:** (1) לזהות פונקציית רינדור + מבנה HTML נוכחי → (2) מוקאפ השוואתי באותו
`docs/mockup-apple-fitness.html` (או קובץ נפרד למסך) → (3) אישור כיוון → (4) יישום ב-CSS/HTML/JS + bump גרסה.

---

## 8. החלטות (נסגרו)

1. ✅ **שילוב כערכת theme נוספת** — Apple Fitness = `data-theme="apple"` חדש. המשתמש בוחר; **Liquid Obsidian
   נשאר ברירת המחדל**. יתרון: אפס רגרסיה — כל הטוקנים נדרסים תחת ה-attribute בלבד, כמו שאר ה-themes.
2. ✅ **ליים = accent גלובלי** — תחת theme "apple", `--accent` (ונגזרותיו `--accent-rgb`, `--accent-gradient`,
   `--accent-soft`...) נדרסים ל-ליים `#B5FF2B`. כפתורים, טבעות, טאב פעיל — ליים. הכחול יורד לתפקיד משני
   (מרחק/קישורים). **הערה:** ליים על טקסט לבן = ניגודיות נמוכה → על כפתורי ליים הטקסט **שחור** (כמו Apple).
3. ⏳ **טאב-בר עם אייקונים?** — עדיין פתוח, יוכרע במסך ה-dashboard (הטאב-בר הגלובלי).
4. **היקף** — מסך אחר מסך לפי ה-Roadmap (סעיף 7), החל ממסך הבית.

### מימוש ה-theme (הנחיה ליישום)
```css
html[data-theme="apple"]{
  --bg:#000; --surface-2:linear-gradient(180deg,#1f1f22,#161618); /* כרטיס גלאס */
  --accent:#b5ff2b; --accent-rgb:181,255,43; --accent-text:#000; /* טקסט שחור על ליים */
  --success:#b5ff2b; --danger:#ff2d55; /* ... שאר הטוקנים לפי סעיף 4 */
}
```
> יש לוודא שכל מקום שמניח `--accent-text:#fff` עדיין קריא — תחת apple ה-accent בהיר, לכן טקסט שחור.

---

## גרסת מסמך
v4 · מוקאפים: סיכום אימון ✅ + מסך בית/dashboard ✅. החלטות ליבה נסגרו (theme "apple" + ליים accent).
**הבא:** מסך אימון פעיל (`ui-main`) — pickers + session strip.
